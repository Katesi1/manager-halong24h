# CLAUDE.md — Webhalong24h Manager

> Tài liệu này điều phối Claude Code (và mọi AI agent) khi làm việc trên dự án.
> **Mọi đổi schema/cấu trúc folder/quy tắc ⇒ cập nhật file này luôn.** AI phải đọc lại file này ở đầu mỗi session.

---

## 1. Stack & Run

| | |
|---|---|
| Framework | **Next.js 15** (App Router, RSC, Server Actions) |
| UI runtime | **React 19** (`useActionState`, `useOptimistic`, `useFormStatus`) |
| Ngôn ngữ | TypeScript 5.7, strict mode |
| Style | Tailwind v4 (`@tailwindcss/postcss`), Radix UI, lucide-react, framer-motion |
| Validation | **Zod 3** (mọi boundary) |
| Form | react-hook-form + Zod resolver (khi cần phức tạp), Server Action + `useActionState` (khi đơn giản) |
| Data source | **REST API ngoài** — base URL: `http://api.halong24h.com` (BE chạy không có prefix `/api/v1` mặc dù spec ghi vậy). Lớp Mock đã loại bỏ — mọi module dùng API thật |
| Dev port | **3001** (`npm run dev`) |

```bash
npm run dev        # dev server
npm run typecheck  # tsc --noEmit
npm run lint       # next lint
npm run build      # next build
```

---

## 2. Kiến trúc — Clean Architecture

```
src/
├── core/                  # 🟢 Domain Layer (pure, không phụ thuộc gì)
│   ├── entities/          # User, Property, Booking, Permission, ...
│   ├── value-objects/     # VND, Email, Phone, RoleCode, PropertyType
│   └── errors.ts          # DomainError + subclasses
│
├── application/           # 🟡 Application Layer (use cases + ports)
│   ├── ports/             # Interface repository (auth/property/dashboard/...)
│   ├── auth/              # login, refresh, getProfile, logout (use case)
│   ├── properties/        # list, getById, create, update, delete, prices, images
│   ├── dashboard/         # getStats, getReports
│   └── result.ts          # Result<T, E> discriminated union
│
├── infrastructure/        # 🔴 Infrastructure Layer (chi tiết kỹ thuật)
│   ├── http/              # api-client, token-storage, api-config, error mapping
│   ├── repositories/      # ApiXxxRepository — concrete impl của ports
│   └── container.ts       # DI: resolve mỗi port về ApiXxxRepository (REST API thật)
│
├── app/                   # 🔵 Presentation (Next.js App Router)
│   ├── (admin)/, (host)/, (auth)/   # Route groups
│   ├── actions/           # Server Actions — entry point của UI vào Application
│   └── auth/              # OAuth callback, signout
│
├── components/            # 🔵 Presentation (React)
│   ├── ui/                # Primitive (Button, Input, Dialog, ...)
│   ├── layout/, common/
│   ├── admin/, host/      # Feature components
│   └── auth/, chat/
│
└── lib/                   # 🛠 Generic utilities (KHÔNG đụng business logic)
    └── format.ts, calendar.ts, pricing.ts, vietqr.ts, utils.ts, result.ts
```

### Quy tắc phụ thuộc (Dependency Rule)

```
core ◄── application ◄── infrastructure
                    ◄── app/components (presentation)
```

- `core` **KHÔNG** import từ bất kỳ layer nào khác.
- `application` chỉ import `core`. Phụ thuộc Infrastructure qua **interface (port)**.
- `infrastructure` import `core` + `application/ports`, implement port.
- `app` (route/page) gọi **Server Action**, Server Action gọi **use case**, use case gọi **port** (Container resolve về `ApiXxxRepository`).

**KHÔNG được** import trực tiếp:
- `infrastructure/*` từ Server Component / Client Component (luôn đi qua Server Action).
- `core/entities/*` từ Client Component nếu entity có method có thể bundle code lớn — nhưng type thì ok.

---

## 3. API Integration (Module 1–3)

**Base URL**: `http://api.halong24h.com` (env: `NEXT_PUBLIC_API_BASE_URL`)
**Swagger**: `http://api.halong24h.com/api` *(spec ghi `/api/v1` nhưng BE thực tế không có prefix — đã verify bằng curl)*
**Spec đầy đủ**: [`api-spec-website-admin.md`](./api-spec-website-admin.md)

### Module đã có API (TÍCH HỢP THẬT)

| Module | Path prefix | Use cases |
|---|---|---|
| **Auth** | `/auth/*` | login, register, googleLogin, refresh, forgotPassword, resetPassword, getProfile, updateProfile (`PATCH /auth/profile`), logout, changePassword |
| **Properties** | `/properties/*` | list, getById, create, update, delete, updatePrices, uploadImages, deleteImage, setCover |
| **Dashboard** | `/dashboard/stats`, `/reports` | getStats, getReports |
| **Bookings** | `/bookings` (GET) | list, getById |
| **Notifications** | `/notifications`, `/notifications/unread-count`, `/notifications/:id/read`, `/notifications/read-all` | list, unreadCount, markRead, markAllRead |

### Module CHƯA có endpoint BE đầy đủ

Một số thao tác chưa có endpoint thật (vd: `PATCH /auth/profile`, đổi role `PATCH /admin/users/:id/role`) → Server Action tương ứng hiện trả mock-success (xem Nhóm B trong changelog `mock-layer-removal`). Khi BE ra endpoint, wire thẳng vào action — UI không phải sửa.

→ Lớp **MockXxxRepository swappable đã bị loại bỏ hoàn toàn**. Container luôn resolve về `ApiXxxRepository`. Không còn env `NEXT_PUBLIC_DATA_MODE_*`.

### Authentication flow

- `accessToken` 15 phút, `refreshToken` 7 ngày — lưu **httpOnly cookie** server-side (qua `next/headers` `cookies()`).
- HTTP client tự thêm `Authorization: Bearer <accessToken>`.
- 401 → use case `refreshTokens()` → retry.
- 403/refresh fail → throw `UnauthorizedError` → Server Action redirect `/login`.

### Response format (theo spec)

```ts
// Success
{ success: true; message: string; data: T }
// Error
{ success: false; statusCode: number; message: string; errors: unknown; path: string; timestamp: string }
```

→ HTTP client unwrap thành `Result<T, ApiError>` cho use case.

---

## 4. Quy ước code

> Chuẩn đầy đủ ở [`docs/CODING-STANDARDS.md`](./docs/CODING-STANDARDS.md) (SOAP, RISE, design patterns).

### Tóm tắt quan trọng

- **Result type** — Server Action không throw, trả `Result<T>`:
  ```ts
  type Result<T, E = string> =
    | { ok: true; data: T }
    | { ok: false; error: E; fieldErrors?: Record<string, string[]> }
  ```
- **VND brand type** — tiền tệ là `VND` không phải `number`.
- **RBAC ở `layout.tsx`** — không dùng middleware làm RBAC chính.
- **Validation Zod** ở mọi Server Action input.
- **File ≤ 400 dòng**, function ≤ 50 dòng, nesting ≤ 4 cấp.
- **No `any`, no `console.log`, no `as any` không có lý do.**

### Naming

- File: kebab-case (`api-auth-repository.ts`)
- Component: PascalCase (`BookingTable.tsx`)
- Hook: camelCase với prefix `use` (`useDebounce.ts`)
- Type/Interface: PascalCase (`type LoginInput`, `interface Property`)
- Boolean: prefix `is/has/can/should`

---

## 5. Roles & Permissions (theo spec)

| Code | Role | Truy cập |
|---|---|---|
| 0 | `ADMIN` | Toàn bộ `(admin)/*` |
| 1 | `OWNER` | `(host)/*`, scope `ownerId === user.id` |
| 2 | `SALE` | `(host)/*`, scope `ownerId === user.ownerId` |
| 3 | `CUSTOMER` | **CHẶN** — không vào trang quản lý |

### Quy tắc UI

- ADMIN bypass mọi check.
- OWNER/SALE: button CRUD chỉ hiện khi `permissions[module].canX === true`.
- KYC: `kycStatus !== 'approved' && !kycBypass` ⇒ banner cho OWNER + chặn create/update/delete property.
- SALE chưa được gán (`ownerId === null`) ⇒ empty state.

---

## 6. Workflow khi thêm feature

Áp dụng **SOAP** trong commit/PR và **RISE** khi giao việc cho AI (xem [`docs/CODING-STANDARDS.md`](./docs/CODING-STANDARDS.md)).

Trình tự cho 1 use case mới:

```
1. core/entities/<X>.ts        — model thuần
2. application/ports/<x>-repository.ts  — interface
3. application/<module>/<action>.ts     — use case (input/output type, gọi repo qua port)
4. infrastructure/repositories/api-<x>-repository.ts  — gọi REST API
5. infrastructure/container.ts — đăng ký (return new ApiXxxRepository())
6. app/actions/<module>.ts     — Server Action gọi use case, parse Zod
7. app/(admin|host)/.../page.tsx — Server Component fetch, render
8. components/<module>/...     — UI client
```

---

## 7. Data Source — chỉ API thật

Dự án thật: **không còn lớp Mock**. Container resolve mọi port thẳng về `ApiXxxRepository`, không có env `NEXT_PUBLIC_DATA_MODE_*`:

```ts
export function bookingRepository(): BookingRepository {
  return new ApiBookingRepository()
}
```

Mọi gọi đều tới `http://api.halong24h.com`. Thao tác nào BE chưa có endpoint thì Server Action trả mock-success tạm thời (xem changelog `mock-layer-removal` Nhóm B) — wire API thật khi BE sẵn sàng.

---

## 8. Self-Update Rule (BẮT BUỘC)

AI agent phải **tự động cập nhật CLAUDE.md** khi:

1. Tạo entity mới ⇒ cập nhật danh sách trong mục **§2** và **§6**.
2. Tích hợp endpoint mới (BE ra endpoint cho thao tác đang mock-success) ⇒ wire API vào Server Action + cập nhật §3.
3. Đổi cấu trúc folder ⇒ cập nhật cây thư mục §2.
4. Thêm pattern/quy ước mới ⇒ ghi vào `docs/CODING-STANDARDS.md`, link từ §4.
5. Đổi data flow (auth, RLS, RBAC) ⇒ cập nhật §3 hoặc §5.

**Format cập nhật**: thêm dòng vào **§11 Changelog** với ngày + 1 dòng mô tả.

---

## 9. Data Source

Project dùng **REST API** qua Clean Architecture: mọi port → `ApiXxxRepository` (REST API). Lớp Mock đã loại bỏ hoàn toàn (xem §7).

Supabase đã được loại bỏ hoàn toàn (code, config, migrations, packages). Không còn bất kỳ reference nào trong source code.

---

## 10. Trạng thái module (đều dùng ApiXxxRepository)

Lớp Mock đã loại bỏ — mọi module gọi API thật. Bảng dưới ghi độ phủ endpoint thực tế của BE:

| Module | Endpoint | Ghi chú |
|---|---|---|
| Auth | ✅ `/auth/*` (+ `PATCH /auth/profile`) | — |
| Properties | ✅ `/properties/*` (+ moderation `/approve` `/reject` `/suspend` + `PATCH /:id/hot`) | Luồng duyệt cơ sở 4-trạng thái đã khôi phục |
| Dashboard | ✅ `/dashboard/stats` + `/reports` + `/reports/risk-kpis` | — |
| Bookings | ✅ `/bookings` (list/detail + hold/confirm/paid/cancel) | — |
| Notifications | ✅ `/notifications` + `unread-count` + `markRead` | — |
| Calendar | ✅ `/calendar/*` (+ `POST /calendar/bulk`) | — |
| Subscriptions | ✅ `/admin/subscriptions` + `/admin/users/:id/subscription/*` + `/subscriptions/me/invoices` + `/admin/subscriptions/:id/call-log` | — |
| Payment sessions | ✅ `/admin/payments` + `/admin/payments/:id/mark-paid` | — |
| STK nền tảng (mua gói) | ✅ `GET/PUT /admin/payments/receiving-bank` (§10.7 v1.21) | ADMIN sửa STK cty thu tiền mua gói, không duyệt |
| Disputes | ✅ `/disputes` + `/admin/disputes/*` (evidence/verdict/chatExcerpts/penalty live §25.8) | — |
| KYC (admin) | ✅ `/admin/kyc/*` + `GET /admin/kyc/:id` (verificationFields[7] + uploads + payments §25.9) | — |
| Bank accounts (STK nhận tiền) | ✅ `/users/me/bank` (OWNER) + `/admin/bank-accounts` + `/admin/users/:id/bank/(approve\|reject)` (§3.3 v1.21) | Luồng duyệt STK nhận cọc → sinh VietQR |
| Reviews | ✅ `/admin/reviews` + `/properties/:id/reviews` | — |
| Permissions / Users (admin) | ✅ `/permissions/:userId` (18 module theo scope §26.5) + `/users?withStats=true&scope=` (stats.disputeCount + lastActiveAt) + `PATCH /users/:id/role` | — |
| System SALE (admin) | ✅ `GET /users?scope=system` + `POST /users {role:2, scope:'system'}` + `PUT /users/:id` + `DELETE /users/:id` (§26 + §3) | Trang `/admin/system-staff`, chỉ ADMIN |
| Staff invite accept (public) | ✅ `GET /staff/invites/verify/:token` + `POST /staff/invites/accept` (§11.2) | Trang `/staff/accept` (public) — nhân viên nhận lời mời tạo SALE bằng Google/email; entry ở trang login |
| Guests | ✅ `/guests` + `/guests/:id` | — |
| Leads | ✅ `/leads` | — |
| Chat / Messages | ✅ `/conversations/*` (REST) + Socket.IO `/chat` | — |
| **Du thuyền (Yacht)** | ✅ `/yachts/*` (CRUD + `/prices` + `/images`) + `/yacht-bookings` (list/detail + confirm/paid/cancel + staff đặt hộ) + `/conversations/yacht` (tin nhắn) + `/admin/yacht-reviews` (+ `/yachts/:id/reviews/:reviewId/reply`) | **ADMIN + SALE hệ thống** (role=2, scope=system — `canManageYachts`, guard `requireYachtManager`). Chat yacht: quản trị không phải member → **poll 15s** thay WS. Booking status 0..4 (0=PENDING 1=CONFIRMED 2=PAID 3=COMPLETED 4=CANCELLED, cron tự lên COMPLETED) |

**Thao tác BE vẫn chưa có endpoint** (Server Action còn mock-success): housekeeping (module "Sắp ra mắt"). Khi BE bổ sung, wire thẳng vào Server Action + cập nhật bảng này.

---

## 11. Changelog

| Ngày | Người | Thay đổi |
|---|---|---|
| 2026-07-20 | admin-upgrade-owner-plan | **Admin nâng gói cho Owner (ghi nhận tiền mặt / CK ngoài app) — trang chi tiết người dùng `/admin/users/:id`.** BE (báo cáo 2026-07-20) đã có sẵn API, FE chỉ làm UI. Wire `POST /admin/users/:id/subscription/mark-paid` với **đầy đủ body** (trước chỉ gửi `{amount}`): (1) Entity [subscription.ts](src/core/entities/subscription.ts) `MarkPaidInput` thêm `planId`/`cycle`/`rooms`/`days`/`reference`/`note` (optional — bỏ trống thì BE giữ giá trị hiện tại) + type `MarkPaidResult`. (2) Repo [api-subscription-repository.ts](src/infrastructure/repositories/api-subscription-repository.ts) `markPaid` build body chỉ đính field có giá trị; `SpecSubscription` + `mapSubscription` đọc thêm response nâng gói (`userId`/`startsAt`/`endsAt` → id/ownerId/startAt/expireAt fallback). (3) Use case [subscriptions/actions.ts](src/application/subscriptions/actions.ts) `MarkPaidSchema`: `paidAmount` đổi `nonnegative`→**`positive`** (BE bắt >0) + 6 field mới (planId ≤64, cycle enum, rooms 1–10k, days 1–3650, reference ≤200, note ≤2000). (4) Server action mới [subscriptions.ts](src/app/actions/subscriptions.ts) `upgradeOwnerSubscriptionAction(userId, {amount, planId, cycle, rooms, days, reference, note})` (`requireAdmin` + uuid-validate + revalidate `/admin/payments` + `/admin/users/:id`). (5) UI mới [subscription-upgrade-button.tsx](src/components/admin/subscription-upgrade-button.tsx): nút "Nâng gói / Ghi nhận thanh toán" mở dialog — load song song `listBillingPlansAction` (chỉ gói active) + `getSubscriptionAction` (gói hiện tại tham chiếu), prefill gói/chu kỳ/số phòng/ngày (30/365) + gợi ý số tiền theo giá niêm yết, ô số tiền format phân cách nghìn, mã biên nhận + ghi chú optional; submit → toast "hạn mới đến …" + `router.refresh()` + `refetchApiResources()`. Gắn vào section "Xác minh KYC & Gói cước" trong [admin-user-detail-client.tsx](src/components/admin/admin-user-detail-client.tsx) (chỉ OWNER). Lỗi BE (amount≤0/user không OWNER/tài khoản khoá/plan không tồn tại/double-submit 409) hiện inline trong dialog. **Case 0đ miễn phí chưa hỗ trợ** (BE cần bổ sung — mục 6 báo cáo). Typecheck **0 errors**, lint 0 warning mới, build OK, đã deploy PM2. |
| 2026-07-20 | kyc-bypass-lock-approved | **Khoá phần "Quyền bỏ qua KYC" ở `/admin/users/:id` khi KYC đã duyệt.** [kyc-bypass-toggle.tsx](src/components/admin/kyc-bypass-toggle.tsx) thêm prop `kycStatus`; khi `approved` → early-return badge "Đã xác minh KYC" + ghi chú, không hiện nút cấp/thu hồi (chủ nhà đã xác minh đầy đủ nên quyền bỏ qua vô nghĩa). [admin-user-detail-client.tsx](src/components/admin/admin-user-detail-client.tsx) truyền `kycStatus={user.kycStatus}`. Các trạng thái `none`/`pending`/`rejected` giữ toggle bình thường. Typecheck 0 errors, đã deploy PM2. |
| 2026-07-13 | yacht-per-person-pricing | **Du thuyền chuyển sang BÁN THEO ĐẦU NGƯỜI (BE §27.2 v1.40.3) — người lớn + trẻ em giá RIÊNG, 3 mức ngày mỗi loại.** Công thức BE: `totalAmount = adults × giá_NL(ngày) + children × giá_TE(ngày)`; bỏ hẳn `adultSurcharge`/`childSurcharge`/`standardGuests`/`standardChildren`; trẻ em chưa cấu hình giá = miễn phí (0đ). **FE web quản lý**: (1) Entity [yacht.ts](src/core/entities/yacht.ts): `Yacht` bỏ `adultSurcharge`/`childSurcharge`/`standardGuests`/`standardChildren`, thêm `weekdayChildPrice`/`weekendChildPrice`/`holidayChildPrice` (VND\|null, null=miễn phí); `CreateYachtInput` + `UpdateYachtPricesInput` đổi tương ứng (3 giá NL bắt buộc + 3 giá TE optional). (2) Zod [yachts/actions.ts](src/application/yachts/actions.ts): `CreateSchema`/`PricesSchema` bỏ 4 field cũ, thêm 3 giá trẻ em optional. (3) Form [yacht-form.tsx](src/components/admin/yacht-form.tsx): section giá tách **2 nhóm** — "Người lớn (₫/khách · bắt buộc)" 3 ô + "Trẻ em (₫/khách · bỏ trống = miễn phí)" 3 ô (placeholder "Miễn phí", omit khi trống); **bỏ ô phụ thu + khách/trẻ tiêu chuẩn**; section Sức chứa còn cabin/sức chứa tối đa/chiều dài. (4) Card [yachts-browser.tsx](src/components/admin/yachts-browser.tsx): giá hiện "từ X**/khách**" (minPrice = giá NL ngày thường). (5) `totalAmount` đơn do BE tính theo đầu người — FE đọc thẳng (không tự cộng phụ thu), không đổi trang đơn. **Repo** [api-yacht-repository.ts] pass-through input nên tự gửi 6 field mới, không cần map. Verify: `PUT /yachts/:id/prices` nhận 6 field. Typecheck **0 errors**, lint 0 warning mới, build OK, đã deploy PM2. **Lịch giá du thuyền** (`/calendar` trả `price`/`childPrice`/`priceType`) vẫn chưa dựng ở web quản lý. |
| 2026-07-13 | yacht-day-tour-model | **Du thuyền chuyển sang mô hình TOUR TRONG NGÀY (BE §27 v1.40.2) — cập nhật web quản lý.** Mỗi tour = 1 sản phẩm (tàu có 2 sản phẩm: tour ngày + tour dinner), đặt tour chỉ cần 1 ngày (`checkoutDate` optional, BE tự lấy = checkinDate), giá tính **theo 1 buổi/đơn** (không nhân đêm), cho nhiều đơn cùng ngày (tour ghép), `checkInTime`/`checkOutTime` = giờ đón/trả khách. **FE web quản lý**: (1) [yacht-form.tsx](src/components/admin/yacht-form.tsx) đổi nhãn cho đúng mô hình: "Giờ check-in/out" → **"Giờ đón/trả khách"**, "Thời lượng hành trình" → **"Thời lượng tour"** (hint "Tour trong ngày · 6–8 giờ / Tour buổi tối · 3–4 giờ"), section "Hành trình" → **"Hành trình trong ngày"** (theo giờ), section giá → **"Bảng giá (mỗi buổi tour)"** + subtitle "giá 1 buổi/đơn, KHÔNG tính theo đêm"; default giờ đón/trả 08:00/16:00. (2) [yacht-bookings-client.tsx](src/components/admin/yacht-bookings-client.tsx) cột "Khởi hành" → **"Ngày đi tour"** (chỉ hiện 1 ngày; range chỉ khi checkout khác ngày checkin — tour trong ngày = 1 ngày). (3) [yacht-booking-detail-client.tsx](src/components/admin/yacht-booking-detail-client.tsx): "Ngày khởi hành/kết thúc" → **"Ngày đi tour"** (ẩn ngày kết thúc khi cùng ngày); **tự hiện QR/STK ngay** khi đơn CONFIRMED — BE giờ trả sẵn `data.payment` trong `GET /yacht-bookings/:id` (thêm `YachtBooking.payment` + mapper đọc `b.payment` [api-yacht-booking-repository.ts](src/infrastructure/repositories/api-yacht-booking-repository.ts)), không cần bấm "Hiển thị lại VietQR" nữa (nút chỉ còn khi thiếu payment). (4) `CreateYachtBookingInput`/Zod đã có `checkOutAt` optional từ trước — khớp mô hình mới, không đổi. **KHÔNG đổi**: kiểm duyệt đánh giá `/admin/yacht-reviews`, luồng confirm→paid. **CHỜ CHỐT nghiệp vụ (BE + sếp)**: giá **per-đơn** (base 2 khách + phụ thu, maxGuests=8) vs **per-person** — nếu chốt per-person thì cơ chế giá + UI phải đổi; hiện GIỮ per-đơn (chưa wire sâu phần giá). Màn lịch giá du thuyền (`/calendar` trả `price`/`priceType`) vẫn chưa dựng. Typecheck **0 errors**, build OK, đã deploy PM2. |
| 2026-07-13 | yacht-be-align + reviews | **Đồng bộ phản hồi BE cho module Du thuyền: mở quyền SALE hệ thống, chốt mapper shape thật, render QR EMV, thêm kiểm duyệt đánh giá + status COMPLETED.** (1) **scope trong `/auth/profile`** (BE xác nhận luôn có, spec §2.5): `AuthUser.scope: 'owner'|'system'|null` ([user.ts](src/core/entities/user.ts)). Helper mới [yacht-access.ts](src/lib/yacht-access.ts) `canManageYachts` (ADMIN ∪ SALE role=2 scope=system) + `isSystemSale`. Guard `requireYachtManager` ([auth-guard.ts](src/lib/auth-guard.ts)). **Mở UI cho SALE hệ thống**: [(admin)/layout.tsx](src/app/(admin)/layout.tsx) cho vào nếu `canManageYachts`; [page.tsx](src/app/page.tsx) root redirect SALE hệ thống → `/admin/yachts`; [manager-sidebar.tsx](src/components/layout/manager-sidebar.tsx) SALE hệ thống chỉ thấy nhóm "Du thuyền" (lọc `ADMIN_GROUPS`), không tab switcher/host. Đổi 6 yacht Server Action `requireAdmin`→`requireYachtManager` + 6 BFF `isAdmin`→`canManageYachts`. **B3 read cho SALE hệ thống**: [yacht-conversations.ts](src/app/actions/yacht-conversations.ts) thêm `getYachtConversationAction`/`listYachtMessagesAction`/`sendYachtMessageAction` gọi thẳng repo (bypass `requireConversationMember` vì quản trị không là member) — BFF `/api/admin/yacht-conversations/[id]` + messages client dùng action mới. (2) **Mapper chốt shape thật** (BE capture prod) [api-yacht-booking-repository.ts](src/infrastructure/repositories/api-yacht-booking-repository.ts): **status number 0..4** (bỏ đoán string), field cố định camelCase `checkinDate`/`checkoutDate`/`totalAmount`/`paidAmount`/`remainingAmount`/`code`/`customerId`(nullable)/`confirmedAt`/`cancelledReason`; `confirm` unwrap `data` (booking) + key `payment`; `paid` trả booking. Entity thêm `paidAt`/`confirmedAt`/`cancelledReason`/`hasReview`/`reviewUnlockAt`/`canReview` + status **`completed`**, bỏ `accessCode` (mã gửi khách = `code`). (3) **QR EMV client-side**: cài `qrcode` + component [qr-code.tsx](src/components/ui/qr-code.tsx) render `payment.qrCode` (chuỗi EMV, canvas) — khớp CRC/nội dung sanitize BE; fallback img.vietqr.io khi thiếu. Booking detail: badge "Đã/Chưa đánh giá" (từ `hasReview`), hiện `code` sau PAID, xử lý trạng thái COMPLETED. (4) **Tab COMPLETED** ([yacht-bookings-client.tsx](src/components/admin/yacht-bookings-client.tsx) + [yacht-display.ts](src/lib/yacht-display.ts)), doanh thu gồm paid+completed. (5) **MỚI — kiểm duyệt đánh giá du thuyền** (full stack): [yacht-review.ts](src/core/entities/yacht-review.ts) (6 tiêu chí, reuse `REVIEW_CRITERIA`) + port/repo ([api-yacht-review-repository.ts](src/infrastructure/repositories/api-yacht-review-repository.ts): `GET /admin/yacht-reviews?status=visible\|hidden\|all` + `DELETE /:id{reason}` ẩn + `POST /:id/restore` + `POST /yachts/:id/reviews/:reviewId/reply{reply}`) + use case + action `requireYachtManager` + BFF + trang [/admin/yacht-reviews](src/app/(admin)/admin/yacht-reviews/page.tsx) + client (tab Hiển thị/Đã ẩn/Tất cả, breakdown 6 tiêu chí + ảnh, ẩn nhập lý do/khôi phục/trả lời inline) + sidebar "Đánh giá du thuyền". Verify BE `/admin/yacht-reviews` → 401 (live). (6) **Bỏ qua**: giá theo ngày trong lịch (`/yachts/public/:id/calendar` trả `price`/`priceType`) — web quản lý CHƯA có màn lịch du thuyền, sẽ thêm khi dựng. Typecheck **0 errors**, lint 0 warning mới, build OK (7 trang + 7 BFF), đã deploy PM2. |
| 2026-07-13 | yacht-admin-module | **Module mới "Du thuyền" (chỉ bên quản trị) — full-stack Clean Arch cho B1 CRUD + B2 đơn đặt + B3 tin nhắn (spec BE `/yachts`, `/yacht-bookings`, `/conversations/yacht`).** Xác nhận 3 endpoint live (curl → 401). **Domain**: [yacht.ts](src/core/entities/yacht.ts) (`Yacht` + `YachtImage` + `YachtItineraryStep` hành trình theo chặng + Create/Update/Prices input) + [yacht-booking.ts](src/core/entities/yacht-booking.ts) (`YachtBooking` 4 trạng thái pending→confirmed→paid + cancelled, `YachtBookingPayment` VietQR) + [yacht-display.ts](src/lib/yacht-display.ts) (nhãn/màu badge). **Ports**: [yacht-repository.ts](src/application/ports/yacht-repository.ts) + [yacht-booking-repository.ts](src/application/ports/yacht-booking-repository.ts) (`confirm` trả `{booking, payment}`) + mở rộng [chat-repository.ts](src/application/ports/chat-repository.ts) `listYachtConversations`. **Infra**: [api-yacht-repository.ts](src/infrastructure/repositories/api-yacht-repository.ts) (mapper chuẩn hoá itinerary/arrays/bookingCount) + [api-yacht-booking-repository.ts](src/infrastructure/repositories/api-yacht-booking-repository.ts) (mapper tolerant tên field + status numeric/string; `confirm` unwrap `data.payment`) + `listYachtConversations` trong [api-chat-repository.ts](src/infrastructure/repositories/api-chat-repository.ts) + container `yachtRepository()`/`yachtBookingRepository()`. **Use cases**: [yachts/actions.ts](src/application/yachts/actions.ts) (Zod: code `[A-Za-z0-9_-]`, 5 giá bắt buộc ở prices, itinerary sort/renumber) + [yacht-bookings/actions.ts](src/application/yacht-bookings/actions.ts). **Server Actions** (đều `requireAdmin`): [yachts.ts](src/app/actions/yachts.ts) (CRUD + prices + images), [yacht-bookings.ts](src/app/actions/yacht-bookings.ts) (list/get/create/confirm/paid/cancel), [yacht-conversations.ts](src/app/actions/yacht-conversations.ts) (list; đọc chi tiết/gửi tái dùng `getConversationAction`/`listMessagesAction`/`sendMessageAction` — đã bypass ADMIN). **BFF** `/api/admin/{yachts,yacht-bookings,yacht-conversations}(+/[id])` (guard `isAdmin`). **Trang** (client-fetch qua `useApiResource`): `/admin/yachts` (browser: stats + tab active/ẩn + search + phân trang), `/admin/yachts/new` + `/admin/yachts/[id]` (form đầy đủ có **editor hành trình** thêm/xoá/sắp chặng + tag-input tiện nghi/dịch vụ + bảng giá; tab Thông tin/Ảnh; status bar bật/tắt + xoá mềm), `/admin/yacht-bookings` (+`[id]`: xác nhận→**render VietQR** từ [vietqr.ts](src/lib/vietqr.ts) `vietQRImageUrlFor` + STK copyable → ghi nhận thanh toán (amount optional) → huỷ), `/admin/yacht-messages` (2 cột list + thread, **poll 15s** `refetchApiResources` vì ADMIN không nhận WS, gửi trả lời). **Sidebar**: nhóm mới "Du thuyền" (Ship/Anchor/MessageSquare) trong `ADMIN_GROUPS`. **Quyết định phạm vi**: web quản lý khu `/admin/*` là ADMIN-only (layout guard redirect role≠0 → `/host`) + `UserProfile` **chưa có field `scope`** → FE guard `requireAdmin` (SALE hệ thống role=2 scope=system dùng được API BE nhưng chưa có UI trên web này cho tới khi BE trả `scope` trong `/auth/profile`). Typecheck **0 errors**, lint 0 warning mới, build OK (6 trang + 6 BFF route), đã deploy PM2. **Chờ BE xác nhận runtime shape**: field đơn `/yacht-bookings` (mapper đang tolerant nhiều tên) + response `confirm` (`data.payment` vs phẳng) + `qrCode` EMV (hiện FE tự dựng ảnh QR từ STK+amount+content qua img.vietqr.io). |
| 2026-07-10 | staff-invite-accept-page | **Trang đăng nhập thêm mục "Tôi có mã mời nhân viên" + luồng nhận lời mời SALE (Google hoặc email/password) — khớp App (spec §11.2).** Trước đây BE đã có `GET /staff/invites/verify/:token` + `POST /staff/invites/accept` (public) và repo [api-staff-repository.ts](src/infrastructure/repositories/api-staff-repository.ts) có sẵn `verifyInvite`/`acceptInvite` nhưng **KHÔNG Server Action + KHÔNG trang nào dùng** → web quản lý thiếu hẳn luồng nhân viên nhận lời mời (chỉ App có). Full-stack: (1) **🐛 Fix mapper verify sai shape**: BE trả `owner` lồng (`{name, avatar, homestayName}`) + `scope` (§26.5) nhưng repo cast thẳng `StaffInviteVerification` đọc `ownerName`/`ownerEmail` phẳng → luôn `undefined`. Sửa `StaffInviteVerification` (port [staff-repository.ts](src/application/ports/staff-repository.ts)) sang shape phẳng đúng (`ownerName`/`ownerAvatar`/`homestayName`/`scope`) + `verifyInvite` map tolerant nested→phẳng. (2) **Server Actions mới** [staff-invite.ts](src/app/actions/staff-invite.ts) (public, không guard): `verifyStaffInviteAction` (normalize token — full 64-hex giữ nguyên, short code `HL-…` upper; chặn status ≠ pending với message rõ), `acceptInviteWithPasswordAction` (useActionState, Zod name≥2/password≥6/phone optional → `acceptInvite` method=password → `writeTokens`→`getProfile`→redirect `/host`), `acceptInviteWithGoogleAction(token, idToken)` (method=google → finishAccept → trả `redirectTo` cho client push). `X-Client-Type: web` đã gắn global ở api-client. (3) **Trang mới** [/staff/accept](src/app/(auth)/staff/accept/page.tsx) (khớp link email BE `/staff/accept?token=<64hex>` + nhập tay) + client [accept-invite-client.tsx](src/components/auth/accept-invite-client.tsx): auto-verify khi có `?token=`, nếu không thì ô nhập mã `HL-XXXXXX` → verify → hiện card chủ nhà/homestay + Google button (tái dùng [GoogleSignInButton](src/components/auth/google-sign-in-button.tsx)) HOẶC form email/password. Accept invite luôn tạo SALE nên **không cần RoleSelectDialog** (khác login Google thường). (4) [login/page.tsx](src/app/(auth)/login/page.tsx) thêm card "Bạn được mời làm nhân viên?" → link `/staff/accept`. (5) [middleware.ts](src/middleware.ts) publicPaths thêm `/staff/accept`. Verify: `curl /staff/accept` → 200 (public OK). Typecheck **0 errors**, lint 0 warning mới, build OK, đã deploy PM2. |
| 2026-07-09 | reset-password-confirm-field | **🐛 Fix reset mật khẩu luôn bị 400 — BE bắt buộc `confirmPassword` nhưng FE không gửi.** Verify curl: `POST /auth/reset-password` thiếu `confirmPassword` → 400 "Xác nhận mật khẩu tối thiểu 6 ký tự" (chưa tới bước check token); gửi đủ 3 field mới đi tiếp. Fix: [api-auth-repository.ts](src/infrastructure/repositories/api-auth-repository.ts) `resetPassword` gửi `confirmPassword: newPassword` (port không đổi — action đã đảm bảo khớp); `ResetPasswordSchema` ([auth.ts](src/app/actions/auth.ts)) thêm field `confirm` + `.refine` khớp mật khẩu (server-side, không chỉ dựa client); [reset-password/page.tsx](src/app/(auth)/reset-password/page.tsx) hiện thêm `fieldErrors.confirm` từ server. Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-08 | chat-remove-attachment-send | **Bỏ chức năng GỬI ảnh/file trong chat web** (theo yêu cầu user). [chat-thread.tsx](src/components/chat/chat-thread.tsx): gỡ `<AttachmentPicker>` (nút 📎) + state `pendingAttachments` + logic snapshot/rollback/revoke ObjectURL; `handleSend` chỉ còn gửi text (`sendMessageAction({conversationId, content})`); nút Gửi disable khi text rỗng. **Xoá file** `attachment-picker.tsx` (orphan). **Vẫn giữ**: render attachment trong tin NHẬN ([message-bubble.tsx](src/components/chat/message-bubble.tsx) `AttachmentList` — khách gửi ảnh từ app vẫn xem được), `uploadFileAction`/`POST /uploads` (avatar dùng), Zod `sendMessageAction` vẫn nhận `attachments` (không gửi thì thôi — dễ khôi phục sau). |
| 2026-07-08 | messages-sidebar-badge | **Badge tin nhắn chưa đọc LIVE trên mục "Tin nhắn" sidebar.** [manager-sidebar.tsx](src/components/layout/manager-sidebar.tsx): badgeKey mới `messages`; đếm client-side qua `getUnreadCountAction()` (`GET /conversations/unread-count`) trong chính sidebar (client component, nằm trong `ChatSocketProvider` ở cả 2 layout) — refetch khi: mount/đổi trang (vào trang hội thoại delay 1.5s chờ mark-read xong), socket `message:new` + `read:update` (debounce 400ms qua `scheduleUnreadRefresh`). Merge với `badges` server-truyền (`mergedBadges`), khác cơ chế badge admin (server unstable_cache 60s) vì cần realtime. |
| 2026-07-08 | chat-statusbar-declutter | **Bỏ chỉ báo "Đang kết nối..."/"Trực tuyến" (trạng thái socket) khỏi khung chat.** [status-bar.tsx](src/components/chat/status-bar.tsx) bỏ prop `connected`, chỉ render khi có tín hiệu hữu ích (peer online / đang nhập / lỗi) — không có gì thì ẩn hẳn thanh (không chừa khoảng trống). Reconnect socket vẫn do ChatSocketProvider tự lo, user không cần thấy. |
| 2026-07-08 | chat-send-crash-fix | **🐛 Fix crash khi GỬI tin nhắn — `Cannot read properties of undefined (reading 'startsWith')` tại MessageBubble.** Response `POST /conversations/:id/messages` thực tế KHÔNG khớp MessageDto spec §17.3 (thiếu `id` hoặc bọc trong `{message}`) → sau khi gửi thành công, [chat-thread.tsx](src/components/chat/chat-thread.tsx) thay tin optimistic bằng `res.data` nguyên khối → message không có `id` → `message.id.startsWith('local-')` sập trang. Vá 3 lớp: (1) repo [api-chat-repository.ts](src/infrastructure/repositories/api-chat-repository.ts) `sendMessage` unwrap `{message}` + normalize `content/attachments` null; (2) chat-thread **merge** response vào optimistic (`id/content/attachments` fallback về giá trị optimistic khi BE trả thiếu) thay vì thay thế nguyên khối + normalize payload WS `onMessage` trước khi vào state; (3) [message-bubble.tsx](src/components/chat/message-bubble.tsx) `(message.id ?? '').startsWith(...)`. Typecheck **0 errors**, lint sạch. |
| 2026-07-08 | chat-null-message-crash-fix | **🐛 Fix crash trang chat detail `/host/messages/[id]` — `TypeError: Cannot read properties of null (reading 'length')` tại MessageBubble.** BE trả `attachments: null` (và có thể `content: null`) cho một số tin nhắn (tin chỉ file / đã xoá / payload WS tối giản) nhưng [message-bubble.tsx](src/components/chat/message-bubble.tsx) đọc thẳng `message.attachments.length` + `message.content.trim()` → sập cả trang khi mở hội thoại chứa tin như vậy. Fix: normalize đầu component `content = message.content ?? ''` + `attachments = message.attachments ?? []` rồi dùng biến chuẩn hoá ở mọi chỗ (render + edit state); `isDeleted` đổi `!== null` → `!= null` (WS payload thiếu field `deletedAt` không còn bị hiểu nhầm là đã xoá). Typecheck **0 errors**, lint sạch. |
| 2026-07-08 | kyc-status-mapper-fix | **🐛 Fix card "TRẠNG THÁI KYC — Đang cập nhật" ở `/host/settings` + `/host/kyc` — trạng thái giờ đọc đúng từ API.** Root cause: `GET /kyc/status` (spec §9.1 v1.7) trả field **`status`** vocab **camelCase** (`kycSubmitted\|paymentPending\|awaitingApproval\|…`) + `rejectReason` top-level, nhưng [api-kyc-repository.ts](src/infrastructure/repositories/api-kyc-repository.ts) cast thẳng sang `KycStatusResponse` đọc `kycStatus` snake_case → luôn `undefined` → card fallback "Đang cập nhật", `/host/kyc` cũng nhận diện sai trạng thái. Fix: mapper tolerant trong repo (đọc `kycStatus ?? status`, alias camelCase→snake_case, `rejectReason`/`kycBypass` default); entity `KycStatusResponse` thêm `rejectReason?` ([kyc.ts](src/core/entities/kyc.ts)); [kyc-status-card.tsx](src/components/host/kyc-status-card.tsx) lý do từ chối đọc `submission?.rejectedReason ?? rejectReason`; 2 route BFF [settings](src/app/api/host/settings/route.ts) + [kyc](src/app/api/host/kyc/route.ts) hợp nhất `kycBypass` với `profile.kycBypass` (BE /kyc/status có thể không trả field này). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-08 | profile-avatar-upload (v1.22) | **Ảnh đại diện trong "Hồ sơ cá nhân" `/host/settings`.** BE v1.22: `PATCH /auth/profile` whitelist thêm `avatar` (URL https ≤1000, upload trước qua `POST /uploads` Cloudinary — không nhận multipart trực tiếp). FE: port `UpdateProfileInput.avatar?` ([auth-repository.ts](src/application/ports/auth-repository.ts)); `UpdateProfileSchema` thêm avatar (https URL, optional, chỉ gửi khi có giá trị) ([auth.ts](src/app/actions/auth.ts)); [profile-form.tsx](src/components/account/profile-form.tsx) thêm khu đổi ảnh — preview tròn + nút "Đổi ảnh đại diện" → validate image/≤10MB → `uploadFileAction` (reuse §23) → URL vào hidden input `avatar`, lưu cùng nút "Lưu thông tin" (hint amber nhắc bấm lưu), thành công → toast + `refetchApiResources()` + `router.refresh()`; BFF [/api/host/settings](src/app/api/host/settings/route.ts) trả thêm `profile.avatar`; banner [settings-client.tsx](src/components/host/settings-client.tsx) + chip topbar [user-menu.tsx](src/components/layout/user-menu.tsx)/[topbar.tsx](src/components/layout/topbar.tsx) hiện ảnh (fallback chữ cái như cũ). Ảnh render `next/image unoptimized` (Cloudinary đã whitelist remotePatterns). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-08 | login-identifier | **Đăng nhập bằng email HOẶC số điện thoại.** BE `/auth/login` vốn nhận `{identifier, password}` (spec §2, identifier = email/phone — verify curl: cả `identifier` lẫn `email` legacy đều trả 401 sai mật khẩu, không 400) nhưng FE chỉ cho nhập email. Sửa: port `LoginCredentials.email` → `identifier` ([auth-repository.ts](src/application/ports/auth-repository.ts)); `LoginSchema` ([login.ts](src/application/auth/login.ts)) chấp nhận email hợp lệ HOẶC phone `0\d{9,10}` (tự strip khoảng trắng/chấm/gạch trong SĐT); `loginAction` ([auth.ts](src/app/actions/auth.ts)) đọc field `identifier` (fallback `email` cũ); UI [login/page.tsx](src/app/(auth)/login/page.tsx) đổi label "Email hoặc số điện thoại", input `type=text` + `autoComplete=username`. Forgot-password đã hỗ trợ identifier từ trước, không đổi. Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-07 | property-moderation-live + refetch-fix | **BE bật lại tạo phòng = `pending` (không auto-approve) → tab "Chờ duyệt" có dữ liệu thật. Wire đầy đủ luồng duyệt + vá lỗi refresh do client-fetch.** (1) **🐛 CRITICAL refresh sau mutation**: sau khi chuyển client-fetch, `router.refresh()` KHÔNG refetch `useApiResource` (dep `[url]` không đổi) → duyệt/từ chối/mọi thao tác admin xong list/detail **stale tới khi F5**. Fix gốc trong [use-api-resource.ts](src/lib/use-api-resource.ts): thêm `refetchApiResources()` (dispatch event `app:data-changed`) + hook lắng nghe → **refetch nền giữ data cũ (không nháy spinner)** nhờ `loadedUrlRef` phân biệt lần đầu vs refetch. Wire `refetchApiResources()` (kèm `router.refresh()` cho badge server-render) vào **21 component mutation** admin+host (approval-actions, hot-toggle, bank/kyc/dispute/review/subscription/user/system-sale row-actions, mark-session-paid, receiving-bank, billing-plan-editor, booking-actions, calendar-grid, lead-actions, staff-row-actions, open-dispute) → giờ duyệt xong list tự cập nhật ngay. (2) **Badge "Chờ duyệt" ở sidebar**: `PropertyFilters` thêm `moderationStatus` ([property.ts](src/core/entities/property.ts)) + repo query `?moderationStatus=` ([api-property-repository.ts](src/infrastructure/repositories/api-property-repository.ts)); action mới `countPendingPropertiesAction` ([admin.ts](src/app/actions/admin.ts), `requireAdmin`, lỗi→0); badgeKey `properties` ([manager-sidebar.tsx](src/components/layout/manager-sidebar.tsx)) trên mục "Toàn bộ cơ sở"; [(admin)/layout.tsx](src/app/(admin)/layout.tsx) `cachedCountPendingProperties` (unstable_cache 60s) → badge (cùng cơ chế 60s như kyc/bank). (3) **Host hiện đúng "chờ duyệt"** (không báo "đã đăng công khai"): [property-edit-client.tsx](src/components/host/property-edit-client.tsx) — header badge theo `moderationStatus` (Chờ duyệt/Từ chối/Tạm ngưng thay vì chỉ isActive), banner sau khi tạo đổi từ "✓ Đã tạo…nhận booking" (xanh) → "⏳ Đang chờ quản trị viên duyệt…" (amber) + banner pending khi vào lại + banner đỏ hiện **lý do từ chối** (owner sửa gửi duyệt lại). **Đã có sẵn từ trước (verify OK)**: tab + stat "Chờ duyệt" trong [properties-browser.tsx](src/components/admin/properties-browser.tsx); nút Duyệt/Từ chối [approval-actions.tsx](src/components/admin/approval-actions.tsx) đúng endpoint `POST /properties/:id/approve` + `/reject {reason≥5}`; badge moderation ở host list [properties-grid-client.tsx](src/components/host/properties-grid-client.tsx). Typecheck **0 errors**, lint 0 warning mới, build OK, đã deploy. |
| 2026-07-07 | client-fetch-100-final | **Chuyển client-fetch TRIỆT ĐỂ — nốt 8 trang cuối (7 host + chat detail), giờ MỌI trang data-driven đều fetch phía client qua BFF `/api/*`.** Đợt này gỡ hết server-to-server còn sót: (1) **7 trang host** `properties/new`, `bookings/new`, `kyc`, `settings`, `settings/bank`, `settings/subscription`, `properties/[id]/images` → mỗi trang thành shell mỏng + client component (`useApiResource`) + route BFF. Các trang có **cổng chặn** (SALE/KYC/gói cước/entitlement) dùng type chung mới [host-gate.ts](src/lib/host-gate.ts) `HostGate` — route **enforce guard PHÍA SERVER** rồi trả `{gate}` (null=cho phép), client render `<GuardBanner>` hoặc nội dung (chặn vẫn ở server + BE, không hạ bảo mật). Route mới: `/api/host/{property-new-gate,booking-new,kyc,settings,bank,subscription}`; `properties/[id]/images` tái dùng `/api/properties/:id`. (2) **Chat detail `/host/messages/[id]`** (trước cố tình giữ SSR) → route [/api/host/messages/[id]](src/app/api/host/messages/[id]/route.ts) trả `{conv, allConvs, currentUserId, peerUserId, initialMessages, initialNextCursor}`, client [chat-detail-client.tsx](src/components/chat/chat-detail-client.tsx) render layout + `<ChatThread>`. Giờ initial load hiện trong Network tab XHR (`GET /conversations/:id`, `/conversations`, `/messages`), tin realtime tiếp theo vẫn qua WebSocket `/chat`. **CÒN server-side (CỐ Ý, là auth/RBAC — không phải page data)**: `src/app/page.tsx` (redirect gốc theo role) + 2 layout `(admin)`/`(host)` (guard RBAC + profile topbar/sidebar + badge count). Đây là hàng rào bảo mật BẮT BUỘC ở server (CLAUDE.md §5 — RBAC ở layout), chuyển sang client sẽ hạ bảo mật + gây flash. Mọi route BFF verify **401** với cookie giả (token BE giữ httpOnly server-side). Typecheck **0 errors**, lint sạch, build OK, đã deploy. |
| 2026-07-07 | client-fetch-admin-100 | **Hoàn tất 100% khu ADMIN — bổ sung 4 trang còn sót: `/admin` dashboard, `/admin/bookings/[id]`, `/admin/properties/[id]`, `/admin/emails`.** (Lượt trước báo nhầm "admin xong" — thực tế còn 4 trang này.) Dashboard (7 fetch: stats/properties/bookings/kyc/users/disputes/revenue) route `/api/admin/dashboard` tính sẵn số liệu (roleCount/propertyCount/top5) trả gọn → client render alert/stat cards + charts (RevenueBarChart/UserRoleDonut) + lists. bookings/[id] + properties/[id]: route detail single-fetch → client bê JSX + sub-component (ApprovalActions/HotToggle/AmenityList). emails: route trả `{profileEmail, smtpEnabled, templates}` → client list mẫu + TestEmailButton. `/admin/payments/sessions` chỉ redirect → không cần convert. **Tổng: MỌI trang admin (20 route) + host đã client-fetch qua BFF `/api/*`.** Chỉ `/host/messages/[id]` (chat realtime WS) giữ SSR. Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-07 | client-fetch-batch10-host-final | **Đợt 10 (cuối) client-fetch — `/host/calendar` + `/host/messages` (list).** calendar (2 fetch: grid + properties) route gộp `/api/host/calendar?start=&days=&property=` → client bento stats + toolbar (URL) + CalendarGrid; messages list (adapt conversation server-side) → client filter/paginate. **Tiến độ: ~29/~40 — hoàn tất toàn bộ trang list + detail + dashboard/calendar/billing của cả admin lẫn host.** Trang duy nhất còn giữ SSR: `/host/messages/[id]` (chat thread realtime — dữ liệu qua WebSocket + server action, endpoint WS đã hiện trong Network tab WS). **Tổng kết migration**: mọi trang data-driven đã fetch client-side qua BFF route `/api/*` (guard `isManagerRole`/`isAdmin`, token BE giữ ở server) → endpoint hiện trong F12 Network. Hook chung [use-api-resource.ts](src/lib/use-api-resource.ts). Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-07 | client-fetch-batch9-host-heavy | **Đợt 9 client-fetch — `/admin/payments` (nốt admin) + host nặng `/host/billing` + `/host` dashboard.** payments (5 fetch: subs/overdue/paid/sessions/bank) route gộp → client segmented 2-view. billing (4 fetch) + dashboard (5 fetch, 627 dòng — trang lớn nhất) bê toàn bộ render/sub-component/chart vào client component, route `/api/host/{billing,dashboard}` guard `isManagerRole`. Dashboard route trả subset `profile` (role/name/email/kycStatus/kycBypass) cho WelcomeChecklist + reportData. **Tiến độ: 27/~40 — TOÀN BỘ admin (16) + hầu hết host xong.** Chỉ còn 2 trang host: `/host/calendar` (grid), `/host/messages` (chat WS). Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-07 | client-fetch-batch8-admin-detail | **Đợt 8 client-fetch admin — reports + 3 detail (disputes/[id], users/[id], kyc/[id]).** Reports gộp 2 fetch (risk-kpis + disputes) trong route `/api/admin/reports?range=`. 3 trang detail: route `/api/admin/{disputes,users,kyc}/[id]` (guard `isAdmin`, 404 khi không có) → client component bê toàn bộ render + sub-component client (DisputeResolutionForm, KycBypassToggle/UserModeration/UserRoleEditor, Kyc*Section/KycModerationActions), loading/error/not-found ở client thay `notFound()`. **Tiến độ: 24/~40** — **admin gần xong** (chỉ còn `/admin/payments` 2-view). Còn host nặng: dashboard, calendar, messages, billing. Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-06 | client-fetch-batch7-admin | **Đợt 7 client-fetch admin — pricing, bank-accounts, system-staff, properties, users, permissions.** Cùng pattern route `/api/admin/*` (guard `isAdmin`) + client component qua `useApiResource`. Ghi chú: pricing (2 fetch public+admin catalog), bank-accounts/kyc/users (server filter/paginate), properties (route map `toRow` rút gọn payload rồi PropertiesBrowser filter in-memory), system-staff (wrapper fetch → SystemSalesBrowser), permissions (route 2 mode: `?userId=`→editor / list SALE). **Tiến độ: 20/~40** (9 host + 11 admin: bookings, disputes, reviews, kyc, audit-log, pricing, bank-accounts, system-staff, properties, users, permissions). Còn admin: payments, subscriptions, reports + detail; host nặng: dashboard, calendar, messages, billing. Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-06 | client-fetch-batch6-admin-list | **Đợt 6 client-fetch admin — disputes, reviews, kyc, audit-log.** Đều theo pattern: route `/api/admin/{disputes,reviews,kyc,audit-log}` (guard `isAdmin`) → client component (`admin-disputes/reviews/kyc/audit-log-client.tsx`) fetch qua `useApiResource`, giữ tab/search URL-driven, phân trang client (fetch-all: disputes/reviews/audit-log) hoặc server (kyc). Endpoint hiện F12 Network. **Tiến độ: 14/~40** (9 host + 5 admin: bookings, disputes, reviews, kyc, audit-log). Còn admin: users, payments, subscriptions, reports, pricing, properties, bank-accounts, permissions, system-staff + detail; host nặng: dashboard, calendar, messages, billing. Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-06 | client-fetch-batch5-admin-bookings | **Đợt 5 client-fetch — bắt đầu khu ADMIN với `/admin/bookings`.** Route admin đặt dưới `/api/admin/*` + guard **`isAdmin`** (chặt hơn managerRole). [api/admin/bookings](src/app/api/admin/bookings/route.ts) trả toàn bộ booking → [admin-bookings-client](src/components/admin/admin-bookings-client.tsx) (stats tiles + tab URL + ClientPagination, reset trang khi đổi tab). Page thành shell PageHeader + client. **Tiến độ: 10/~40** (9 host + admin/bookings). Còn admin: users, payments, disputes, kyc, reviews, subscriptions, audit-log, reports, pricing, properties, bank-accounts, permissions, system-staff + detail; host nặng: dashboard, calendar, messages, billing. Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-06 | client-fetch-batch4-detail | **Đợt 4 client-fetch — detail `/host/leads/[id]` + `/host/bookings/[id]`.** [api/leads/[id]](src/app/api/leads/[id]/route.ts) + [lead-detail-client](src/components/host/lead-detail-client.tsx) (bê helper calendar/estimate + JSX vào client). [api/bookings/[id]](src/app/api/bookings/[id]/route.ts) tính sẵn `estimatedTotal` server-side (fetch cả property khi HOLD/CONFIRMED) → [booking-detail-client](src/components/host/booking-detail-client.tsx) render (BookingActions/OpenDisputeButton client cũ giữ nguyên). Loading/error/not-found ở client thay `notFound()`. **Tiến độ: 8/~40.** Còn host: messages, calendar, properties/[id], dashboard, billing; + admin ~18. Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-06 | client-fetch-batch3-staff-guestdetail | **Đợt 3 client-fetch — `/host/staff` + `/host/guests/[id]`.** (1) [api/staff](src/app/api/staff/route.ts) trả cả `{staff, invites}` (Promise.all 2 action) → [staff-lists-client](src/components/host/staff-lists-client.tsx) render 2 bảng + ClientPagination; page giữ aside form mời (`InviteStaffForm` client cũ). (2) [api/guests/[id]](src/app/api/guests/[id]/route.ts) (single-fetch, 404 khi không có) → [guest-detail-client](src/components/host/guest-detail-client.tsx) (loading/error/not-found ở client thay `notFound()` server); page thành shell `PageHeader` + client. **Tiến độ: 6/~40** (bookings, properties, guests, leads, staff, guests/[id]). Còn: messages, calendar, dashboard, billing, detail (bookings/[id], properties/[id], leads/[id]), toàn bộ admin. Typecheck **0 errors**, build OK, đã deploy. |
| 2026-07-06 | client-fetch-batch2-host-list | **Đợt 2 migration client-fetch — thêm hook dùng chung + convert host/properties/guests/leads.** Mở các trang này → F12 Network thấy `GET /api/{properties,guests,leads}`. (1) **Hook** [use-api-resource.ts](src/lib/use-api-resource.ts) `useApiResource<T>(url)` (loading/error/data, refetch khi url đổi) — giảm lặp cho các trang sau. (2) Route [api/properties](src/app/api/properties/route.ts) (list, reuse `listPropertiesAction`), [api/guests](src/app/api/guests/route.ts) (phân trang server-side, parse q/label/page/limit), [api/leads](src/app/api/leads/route.ts) (fetch-all) — đều guard `getCurrentProfile`+`isManagerRole`, giữ token server-side. (3) Client [properties-grid-client](src/components/host/properties-grid-client.tsx) (fetch-all + ClientPagination), [guests-table-client](src/components/host/guests-table-client.tsx) (server-paginated, Pagination Link giữ q/label), [leads-list-client](src/components/host/leads-list-client.tsx) (fetch-all + filter/count client + ClientPagination). Page shell giữ header/search/filter URL-driven. Verify token giả→401 JSON. **Tiến độ: 4/~40 trang** (bookings, properties, guests, leads). Typecheck **0 errors**, lint sạch, build OK, đã deploy. |
| 2026-07-06 | client-fetch-bff-bookings | **Chuyển `/host/bookings` sang client-fetch qua BFF route → endpoint hiện trong F12 Network (giống pattern hatigo), KHÔNG lộ token BE.** User muốn thấy endpoint trong Network; kiến trúc RSC/Server Action gọi BE server-side nên browser không thấy. Giải pháp proxy same-origin giữ token ở server: (1) **Route Handler** [api/bookings/route.ts](src/app/api/bookings/route.ts) `GET`: guard `getCurrentProfile`+`isManagerRole` → `listBookingsUseCase` (use case tự đính `Authorization` từ cookie httpOnly gọi `api.halong24h.com`) → trả `{data}` JSON đã map entity. (2) **Client** [bookings-table-client.tsx](src/components/host/bookings-table-client.tsx): `fetch('/api/bookings?status=…')` phía client (loading/error/empty + ClientPagination), refetch khi đổi tab. (3) [host/bookings/page.tsx](src/app/(host)/host/bookings/page.tsx) rút gọn thành server shell (header + tab) + `<BookingsTableClient>`. Mở `/host/bookings` → Network hiện `GET /api/bookings?status=… → 200` cùng origin; token vẫn server-side (verify: no-cookie→307, token sai→401 JSON). **1 trang MẪU** — áp dụng toàn app cần lặp pattern (route + client component) từng trang. Typecheck **0 errors**, lint sạch, build OK, đã deploy. |
| 2026-07-06 | reset-password-page + password-eye | **Hoàn tất luồng quên mật khẩu + icon mắt cho mọi ô mật khẩu.** (1) **Trang mới** [/reset-password](src/app/(auth)/reset-password/page.tsx) — đọc `?token=` từ link email, form mật khẩu mới + ô xác nhận (check khớp client-side), gọi `resetPasswordAction` sẵn có (`POST /auth/reset-password {token,newPassword}`), thành công → `/login?reset=1`; thiếu token → banner lỗi + link gửi lại email. [middleware.ts](src/middleware.ts) thêm `/reset-password` vào `publicPaths`. [login/page.tsx](src/app/(auth)/login/page.tsx) thêm banner xanh "Đã đặt lại mật khẩu thành công" khi `?reset=1`. (2) **Component mới** [password-input.tsx](src/components/ui/password-input.tsx) `<PasswordInput>` — ô mật khẩu có nút con mắt bật/tắt hiển thị (dùng được cả controlled lẫn form thường). Áp vào **mọi** ô mật khẩu: login, signup, [password-form.tsx](src/components/account/password-form.tsx) (3 ô đổi mật khẩu), trang reset (2 ô), [system-sale-dialogs.tsx](src/components/admin/system-sale-dialogs.tsx) (bỏ PasswordInput cục bộ trùng lặp → dùng bản chung). (3) **Alias khớp link email BE**: route mới [auth/reset-password/route.ts](src/app/auth/reset-password/route.ts) — email BE sinh `…/auth/reset-password?token=` → redirect 307 sang `/reset-password` giữ nguyên token; middleware publicPaths thêm `/auth/reset-password`. **BE cần đổi domain trong email template** `https://halong24h.com` → `https://manager.halong24h.com` (path giữ nguyên cũng chạy nhờ alias). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-06 | host-property-toggle-delete | **Trang chỉnh sửa cơ sở host thêm khu "Trạng thái & xoá cơ sở".** Component mới [property-danger-zone.tsx](src/components/host/property-danger-zone.tsx) gắn cuối [property-edit-client.tsx](src/components/host/property-edit-client.tsx) (sau merge client-fetch 2026-07-07, page chỉ còn shell): (1) **Mở/Đóng phòng** — toggle `isActive` qua `updatePropertyAction(id, {isActive})` sẵn có (`PATCH /properties/:id`), ConfirmDialog + toast + `router.refresh()` (badge header cập nhật theo). (2) **Xoá cơ sở** — ConfirmDialog danger "KHÔNG thể hoàn tác" → `deletePropertyAction(id)` sẵn có (`DELETE /properties/:id`) → toast + `router.push('/host/properties')`. Không thêm action/repo mới — chỉ wire UI vào action đã tồn tại (guard `requireOwnerOfProperty` + revalidate sẵn). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-06 | idor-conversations-membership | **Vá IDOR chat — check membership FE cho read hội thoại (hoàn tất Nhóm A).** Trước đây `getConversationAction`/`listMessagesAction` chỉ `requireAuthenticated` → user đăng nhập đoán `conversationId` có thể đọc hội thoại/tin nhắn của người khác (chỉ dựa BE). Helper mới `requireConversationMember(conversationId)` trong [conversations.ts](src/app/actions/conversations.ts): fetch conversation → verify `profile.id ∈ members[].userId` (ADMIN bypass), trả conv để getConversation khỏi fetch 2 lần. `getConversationAction` (trả members/preview = PII) + `listMessagesAction` (trả nội dung tin nhắn) giờ chặn non-member → 403. **Write (send/edit/delete/markRead) giữ `requireAuthenticated` + dựa BE** (hot-path, BE authoritative, tránh fetch-conv mỗi lần gửi). Typecheck **0 errors**, lint sạch, build OK. |
| 2026-07-06 | idor-booking-dispute-ownership | **Vá IDOR Nhóm A — thêm kiểm tra ownership FE cho booking + dispute (defense-in-depth, khớp pattern `requireOwnerOfProperty`).** Trước đây `bookings.ts` (get/hold/confirm/markPaid/cancel) + `openDispute` chỉ `requireManagerRole` → OWNER/SALE đoán UUID có thể đọc/sửa/hủy booking của chủ khác (chỉ dựa BE scope). (1) **Helper mới** [auth-guard.ts](src/lib/auth-guard.ts) `requireOwnerOfBooking(bookingId)`: fetch booking → verify `booking.propertyId` thuộc owner của caller (OWNER dùng `id`, SALE dùng `ownerId`), ADMIN bypass; trả kèm `{profile, booking}` để read tái dùng (không fetch 2 lần). (2) [bookings.ts](src/app/actions/bookings.ts): `getBookingAction`/`confirm`/`markPaid`/`cancel` → `requireOwnerOfBooking(id)`; `holdBookingAction` → validate `propertyId` rồi `requireOwnerOfProperty` (chặn hold trên cơ sở owner khác). (3) [disputes.ts](src/app/actions/disputes.ts) `openDisputeAction` → `requireOwnerOfBooking(input.bookingId)` (chỉ mở dispute trên booking của mình; ADMIN bypass). Trang chi tiết booking (host/admin) đã sẵn `if(!result.ok||data===null) notFound()` → booking không thuộc quyền giờ trả 404 thay vì lộ PII. **Chi phí**: mỗi mutation booking +1–2 BE call (fetch booking + property) để verify — chấp nhận cho thao tác tiền/hủy. **CHƯA vá (rely BE scope, cần quyết định riêng)**: `guests`/`leads` (không có link ownership lộ ra FE) + `conversations` (cần check membership qua `members[]`) — khuyến nghị xác minh BE scope đúng thay vì check FE mong manh. Typecheck **0 errors**, lint 0 warning mới (3 warning `profile` ở disputes là pre-existing), build OK. |
| 2026-07-06 | security-audit-4agent | **Audit bảo mật + validation toàn dự án (4 agent song song: authz / input-validation / injection-secrets / auth-session) + vá 4 lỗ hổng rõ ràng.** (1) **CRITICAL — [billing-plans.ts](src/app/actions/billing-plans.ts) thiếu guard**: `create/update/delete/listAll` không có `requireXxx` (outlier duy nhất — mọi admin action khác đều `requireAdmin`). BE `@Roles(ADMIN)` chặn end-to-end nhưng FE mất lớp defense-in-depth. Fix: thêm `requireAdmin()` vào 4 action (giữ `list` public vì là catalog giá công khai). (2) **money — [markBookingPaid](src/application/bookings/actions.ts)**: `amount<=0` để lọt `NaN` (NaN<=0=false). Fix: `!Number.isFinite(amount)||amount<=0||amount>10^10`. (3) **log PII — [api-booking-repository.ts](src/infrastructure/repositories/api-booking-repository.ts)**: xoá `console.log('[debug booking raw]')` (TEMP DEBUG log nguyên payload booking, dev-gated nhưng để sót). (4) **XSS — [message-bubble.tsx](src/components/chat/message-bubble.tsx)**: attachment đến qua WS không qua Zod của `sendMessageAction` → re-validate scheme `^https://` lúc render, chặn `javascript:`/`data:` vào href/src. **Xác nhận tốt (agent verify)**: RBAC enforce ở layout server-side (không chỉ middleware), cookie httpOnly+secure+sameSite, `sanitizeRedirect()` chặn open-redirect, không secret hardcode, refreshToken không lộ client, remotePatterns tight, không `dangerouslySetInnerHTML`. **Còn tồn (report, chưa vá — cần quyết định/lớn hơn)**: (a) IDOR defense-in-depth ở `bookings.ts`/`openDispute`/`guests`/`leads`/`conversations` — chỉ `requireManagerRole`/`requireAuthenticated`, không check ownership FE (BE scope là hàng rào chính); (b) path-segment id chưa `encodeURIComponent`+`.uuid()` (systemic ~20 repo, rủi ro `../` normalize — cần BE authz đúng); (c) CSP đang Report-Only + `unsafe-inline` (deferred, migrate nonce sẽ vỡ GSI); (d) accessToken serialize vào client bundle cho chat WS (tradeoff đã ghi nhận); (e) `/api/auth/refresh` dead code; (f) validation lẻ: bulk-calendar date/mode, risk-kpis range, `limit` chưa cap ở 4 list admin. Typecheck **0 errors**, lint sạch, build OK. |
| 2026-07-06 | host-buy-plan-review-hardening | **Kiểm duyệt luồng mua gói (typescript-reviewer + security-reviewer) + sửa các finding tiền/bảo mật.** (1) **CRITICAL — catch-all che lỗi**: [purchase.ts](src/application/payments/purchase.ts) trước bắt MỌI lỗi initiate → getActive → hiện phiên bất kỳ (che `subscriptionFrozen`/`downgradeScheduled`/`planNotFound`, có thể hiện phiên gói KHÁC). Fix: repo [api-payment-purchase-repository.ts](src/infrastructure/repositories/api-payment-purchase-repository.ts) chỉ map 409 `code:'paymentPending'` → `PaymentPendingError` mới ([core/errors.ts](src/core/errors.ts)); use case CHỈ fallback getActive khi `PaymentPendingError`, lỗi khác nổi nguyên trạng. (2) **CRITICAL — bypass 0đ**: quote trả thiếu `totalAmount` → default 0 → nhánh `upgrade` BE KHÔNG validate amount → session 0đ. Fix: use case chặn `totalAmount<=0` → ValidationError. (3) **SEC — thao túng giá**: `rooms` do client gửi → có thể forge. Fix: bỏ `rooms` khỏi input action; use case suy `rooms` server-side từ `planId` (`roomsFromPlanId()` [payment-session.ts](src/core/entities/payment-session.ts)). (4) **SEC — authz**: [actions/payments-purchase.ts](src/app/actions/payments-purchase.ts) `requireManagerRole` → `requireOwner` (fail-closed FE, khớp `@Roles(OWNER)` BE, chặn SALE). (5) **SEC — URL**: `vietQRImageUrlFor` [vietqr.ts](src/lib/vietqr.ts) `encodeURIComponent` cho bankBin/accountNumber (path segment). (6) **UX — STK thiếu**: [tier-selector.tsx](src/components/host/tier-selector.tsx) `PaymentSessionView` khi thiếu bankBin/STK → banner đỏ "Chưa cấu hình TK nhận tiền, liên hệ Halong24h" thay vì gạch ngang + số tiền; banner paymentPending hiện tên gói của phiên (tránh nhầm gói). **Xác nhận tốt (giữ, không regress)**: `totalAmount` luôn từ quote server-side (client KHÔNG set được số tiền), không nhận `userId` (không IDOR), không log secret/PII. **Defer (note)**: render `vietQrPayload` gốc qua QR-lib thay img.vietqr.io (hiện dựng lại từ bin+stk+amount+memo, khớp ckContent → đối soát vẫn đúng); countdown `qrExpiresAt`; humanize mã lỗi §10.2. Typecheck **0 errors**, lint sạch, build OK. |
| 2026-07-06 | host-buy-plan-real-qr (§10.2) | **🐛 Sửa `/host/billing` khi chọn gói hiện STK giả + không có QR → wire luồng mua gói thật `POST /payments/quote` + `/payments/initiate`.** Trước đây [tier-selector.tsx](src/components/host/tier-selector.tsx) `PlanTransferInfo` **hardcode** STK (Vietcombank/1234567890/CONG TY HALONG24H) + nội dung `GOI <planId>` — sai kiến trúc (spec §10 checklist: "STK lấy từ POST /payments/initiate (session.bankInfo)"). **Full-stack Clean Arch**: (1) **Entity** [payment-session.ts](src/core/entities/payment-session.ts) thêm `PaymentBankInfo` + `PaymentInitiateResult` (bankInfo THẬT đồng bộ từ STK admin cấu hình §10.7 + `ckContent=HALONG24H <sessionId>` + `totalAmount` đã gồm VAT + qrExpiresAt). (2) **Port** [payment-purchase-repository.ts](src/application/ports/payment-purchase-repository.ts) quote/initiate/getActive + **repo** [api-payment-purchase-repository.ts](src/infrastructure/repositories/api-payment-purchase-repository.ts) (`POST /payments/quote`, `POST /payments/initiate {planId,cycle,method:bank_transfer,rooms,totalAmount}`, `GET /payments/active`; mapper tolerant). Container `paymentPurchaseRepository()`. (3) **Use case** [application/payments/purchase.ts](src/application/payments/purchase.ts): quote → initiate (dùng totalAmount từ quote để tránh 400 `amountMismatch`); nếu BE trả `paymentPending` (đã có phiên chờ) → `getActive()` hiện lại phiên đó (`alreadyPending=true`) thay vì báo lỗi cụt. (4) **Server Action** [actions/payments-purchase.ts](src/app/actions/payments-purchase.ts) `startPlanPaymentAction` (`requireManagerRole`; BE enforce OWNER-only). (5) **QR helper** [vietqr.ts](src/lib/vietqr.ts) `vietQRImageUrlFor()` sinh URL img.vietqr.io từ bankBin+STK+amount+memo (đã whitelist remotePatterns+CSP; trả null nếu thiếu BIN/STK). (6) **UI** `PlanTransferInfo` rewrite: mount 1 lần (ref-guard, không lặp) gọi action → loading spinner → hiện STK thật + số tiền (đã VAT) + nội dung CK + **ảnh QR VietQR** (`PaymentSessionView`); enterprise/giá 0 giữ nhánh "Liên hệ". Verify endpoint BE: `/payments/quote|initiate|active` đều trả 401 (tồn tại, path đúng). **Chưa test được luồng auth đầy đủ headless** (cần OWNER đã duyệt KYC) — logic khớp spec §10.2, mapper tolerant field. Typecheck **0 errors**, lint sạch, build OK. |
| 2026-07-06 | audit-effect-loops | **Rà soát toàn codebase lớp lỗi vòng lặp effect / request storm (sau sự cố toast 429).** Quét mọi custom hook + `useEffect` (dep array chứa function/object không ổn định, effect gọi `router.refresh/push/replace` / server action / `setInterval/timeout`, effect gọi `setState` với dep bị chính nó đổi). **Kết luận: chỉ `receiving-bank-panel` từng lặp vô hạn (đã fix gốc bằng memoize `useToast`).** Các chỗ khác đều đúng pattern: `useChatConnection`/`useChatEvents` (handlersRef ổn định, deps `[socket]`), `ChatSocketProvider` (`[token, router]` + `refreshingRef` chặn refresh storm), chat-thread mark-read (chỉ chạy khi `lastMessage.id`/`connected` đổi), property-wizard draft (debounce 500ms + cleanup), property-filters/date-range-picker/user-moderation/google-btn (setState chỉ đổi state nội bộ, không đụng prop trong deps), notification-bell/user-menu/date-picker (`[open]` gắn listener). **Defense-in-depth**: thêm `handledStateRef` vào effect của [receiving-bank-panel.tsx](src/components/admin/receiving-bank-panel.tsx) — mỗi kết quả `state` xử lý đúng 1 lần, chặn lặp kể cả nếu `show`/`router` lỡ đổi ref lần nữa (vì effect này có `router.refresh()`). Typecheck **0 errors**, lint sạch, build OK. |
| 2026-07-06 | fix-toast-infinite-loop | **🐛 CRITICAL — Fix vòng lặp vô hạn khi lưu STK nhận tiền gói (spam popup "cập nhật thành công" + 429 Too Many Requests + bị out tài khoản).** Root cause: [useToast()](src/components/ui/toast.tsx) tạo hàm `show` **mới mỗi render** (không memoize). [receiving-bank-panel.tsx](src/components/admin/receiving-bank-panel.tsx) có `useEffect(() => { if(state.ok){ show(); setOpen(false); router.refresh() } }, [state, show, router])` — `router.refresh()` re-render → `show` ref mới → deps đổi → effect chạy lại (state.ok vẫn true) → **lặp vô hạn**: spam toast + spam `router.refresh()` gọi lại RSC `/admin/payments` (getReceivingBank + subscriptions + profile) → BE **ThrottlerException 429** → `/auth/refresh` bị chặn → `getCurrentProfile` fail → redirect `/login` (out tài khoản). **Fix gốc**: memoize `show` bằng `useCallback([])` (sonner `toast` là singleton ổn định). Sửa 1 chỗ chặn toàn bộ consumer dùng pattern này — các form khác (`bank-account-form`, `profile-form`, `password-form`) đưa `show` vào effect deps cũng hết lặp toast nhẹ. Không đổi logic effect từng component (giữ minimal). Typecheck **0 errors**, lint sạch, build OK, đã deploy PM2. **Lưu ý vận hành**: tài khoản/IP vừa dính 429 có thể bị BE throttle thêm ~1 phút — đợi rồi đăng nhập lại là bình thường. |
| 2026-07-06 | bank-select-dropdown | **Thay ô gõ tay mã BIN bằng dropdown chọn ngân hàng** (tránh gõ sai BIN làm VietQR trỏ nhầm ngân hàng). (1) **Mới** [lib/vn-banks.ts](src/lib/vn-banks.ts) — danh sách 43 ngân hàng NAPAS phổ biến `{bin, name}` xếp theo độ phổ biến + helper `bankNameByBin()`. (2) **Mới** [components/common/bank-select.tsx](src/components/common/bank-select.tsx) — `<BankSelect>` client: `<select name="bankBin">` + `<input hidden name="bankName">` tự điền theo lựa chọn (giữ đúng 2 field FormData mà server action đang đọc → BE không đổi). Giữ option prefill khi BIN cũ không nằm trong danh sách (dữ liệu legacy không mất). (3) Thay 2 ô (BIN gõ tay + tên ngân hàng) bằng 1 `<BankSelect>` ở **cả 2 form**: [receiving-bank-panel.tsx](src/components/admin/receiving-bank-panel.tsx) (STK nền tảng thu tiền gói, admin) + [bank-account-form.tsx](src/components/host/bank-account-form.tsx) (STK cọc OWNER, host). Danh sách `BANK_NAMES` cũ trong [lib/vietqr.ts](src/lib/vietqr.ts) giữ nguyên (chỉ dùng cho helper hiển thị legacy, không phải nguồn của dropdown). Typecheck **0 errors**, lint 0 warning mới, build OK. |
| 2026-07-05 | system-sale-list-permissions (§26.3) | **Bảng "Tài khoản Sale hệ thống" đổi endpoint list sang `GET /admin/system-staff` (trả kèm `permissions[]`) + thêm cột "Quyền" tóm tắt.** BE (§26) khuyến nghị dùng endpoint chuyên dụng `/admin/system-staff` thay `GET /users?scope=system` vì nó hydrate `permissions[]` mỗi user. (1) [api-system-staff-repository.ts](src/infrastructure/repositories/api-system-staff-repository.ts): `list()` gọi `GET /admin/system-staff?isActive=all` (fetch tất cả để browser đếm tab in-memory), bỏ filter `scope==='system'` cứng (BE đã lọc; chỉ lọc phòng thủ khi field có mặt), `mapSale` giữ `permissions[]`. (2) [page.tsx](src/app/(admin)/admin/system-staff/page.tsx): tính `grantedModules` = số module bật ≥1 quyền CRUD (trên tổng 18). (3) [system-sales-browser.tsx](src/components/admin/system-sales-browser.tsx): cột "Quyền" mới — badge `N module` (navy, có quyền) / `Cấp quyền` (xám, chưa cấp) là `<Link>` sang `/admin/permissions?userId=` để sửa nhanh. **Xoá vẫn xoá cứng `DELETE /users/:id`** + **không dùng luồng invite email** (giữ theo quyết định user 2026-07-04 — BE có `DELETE /admin/system-staff/:userId` soft-disable + `POST /admin/system-staff/invites` nếu sau này muốn đổi). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-05 | platform-receiving-bank (§10.7 v1.21) | **STK nền tảng nhận tiền mua gói — ADMIN sửa được qua web (BE lưu DB, hiệu lực ngay, KHÔNG duyệt).** Trước đây STK thu tiền mua gói cố định trong env ACB/21169431/NGUYEN VU NAM; giờ BE có singleton `payment_bank_account` (đọc DB → fallback env). **KHÁC STK cọc OWNER §3.3**: đây là STK của platform, dùng sinh VietQR khi OWNER mua/gia hạn gói, admin ghi thẳng (audit `payment.receiving_bank_update`). Full-stack: (1) **Entity** [platform-bank.ts](src/core/entities/platform-bank.ts) `ReceivingBankAccount` (4 field bank + `source: 'env'\|'db'` + `updatedAt`) + `UpdateReceivingBankInput`. (2) **Port** [platform-bank-repository.ts](src/application/ports/platform-bank-repository.ts) getReceiving/updateReceiving + **use case** [application/platform-bank/actions.ts](src/application/platform-bank/actions.ts) (Zod: BIN 6 số, số TK 6–20 số, tên ≤100 bắt buộc, bankName ≤100 optional). (3) **Repo** [api-platform-bank-repository.ts](src/infrastructure/repositories/api-platform-bank-repository.ts) `GET/PUT /admin/payments/receiving-bank` (mapper tolerant, `source` mặc định 'env'). Container `platformBankRepository()`. (4) **Server Actions** [actions/platform-bank.ts](src/app/actions/platform-bank.ts): `getReceivingBankAction` + `updateReceivingBankAction` (form useActionState), đều `requireAdmin`. (5) **UI** [receiving-bank-panel.tsx](src/components/admin/receiving-bank-panel.tsx) (client): banner amber động (số TK/ngân hàng/chủ TK + badge "Đã cấu hình"/"Mặc định" + ngày cập nhật) + nút "Sửa STK" mở dialog form 4 ô (fieldErrors inline, lưu → toast + refresh). Thay banner ACB **hardcode** + StatCard "TK nhận tiền" trong [payments-approve-view.tsx](src/components/admin/payments-approve-view.tsx) bằng dữ liệu thật; [/admin/payments](src/app/(admin)/admin/payments/page.tsx) fetch `getReceivingBankAction()` trong Promise.all, fallback env-shape khi lỗi. Guard BE cho phép cả SALE có quyền `payments` nhưng khu `/admin` FE là ADMIN-only nên dùng `requireAdmin`. Typecheck **0 errors**, lint **0 warning mới**. |
| 2026-07-05 | bank-account-approval (§3.3 v1.21) | **Module mới "Duyệt tài khoản nhận tiền OWNER" (spec §3.3, BREAKING v1.21).** OWNER tạo/sửa STK ngân hàng nhận cọc (sinh VietQR) **phải qua ADMIN duyệt** mới có hiệu lực — `PUT /users/:id` không còn nhận field bank. Full-stack Clean Arch: (1) **Entity** [bank-account.ts](src/core/entities/bank-account.ts): `BankStatus` (none/pending/approved/rejected), `BankDetails` (bankBin/bankName/bankAccountNumber/bankAccountName, nullable), `BankAccountState` (GET/PUT /users/me/bank), `BankAccountQueueItem`/`BankAccountQueueResult` (admin list), labels + helper. [user.ts](src/core/entities/user.ts) `UserProfile` thêm 4 field bank + `bankStatus` + `bankRejectReason` (BE trả sẵn trong `/auth/profile`, cast trực tiếp). (2) **Port** [bank-account-repository.ts](src/application/ports/bank-account-repository.ts): getMine/submitMine/listQueue/approve/reject/countPending. (3) **Use case** [application/bank-account/actions.ts](src/application/bank-account/actions.ts) — Zod: `bankBin` đúng 6 số, `bankAccountNumber` 6–20 số, `bankAccountName` ≥2; reject reason 5–500. (4) **Repo** [api-bank-account-repository.ts](src/infrastructure/repositories/api-bank-account-repository.ts): `GET`/`PUT /users/me/bank`, `GET /admin/bank-accounts?status&page&limit` (status pending/approved/rejected/all), `POST /admin/users/:id/bank/(approve\|reject)`; mapper tolerant, `pending` chỉ trả khi status='pending'. Container `bankAccountRepository()`. (5) **Server Actions** [actions/bank-accounts.ts](src/app/actions/bank-accounts.ts): `getMyBankAction`/`submitMyBankAction` (requireOwner, form useActionState), `listBankAccountsAction`/`countPendingBankAccountsAction`/`approveBankAccountAction`/`rejectBankAccountAction` (requireAdmin). (6) **ADMIN**: trang mới [/admin/bank-accounts](src/app/(admin)/admin/bank-accounts/page.tsx) — tab lọc (Chờ duyệt/Đã duyệt/Đã từ chối/Tất cả) + badge count + bảng (STK chờ duyệt vs đang dùng) + phân trang; [bank-account-row-actions.tsx](src/components/admin/bank-account-row-actions.tsx) Duyệt (ConfirmDialog hiện STK) + Từ chối (modal nhập lý do), chỉ hiện khi status='pending' (spec 400 bankNoPending). (7) **OWNER**: trang mới [/host/settings/bank](src/app/(host)/host/settings/bank/page.tsx) + [bank-account-form.tsx](src/components/host/bank-account-form.tsx) render 4 trạng thái (none→form, approved→xem+sửa, pending→banner chờ duyệt+xem+gửi lại, rejected→banner đỏ lý do+gửi lại); thẻ liên kết + badge trạng thái trong [/host/settings](src/app/(host)/host/settings/page.tsx). (8) **Wire**: sidebar [manager-sidebar.tsx](src/components/layout/manager-sidebar.tsx) mục "Duyệt tài khoản nhận tiền" (Landmark, badgeKey `bank`) nhóm Tài chính admin + "Tài khoản nhận tiền" nhóm host (hideForSale); [(admin)/layout.tsx](src/app/(admin)/layout.tsx) fetch `countPendingBankAccountsAction` (unstable_cache 60s) → badge; notification deep-link [lib/notification-link.ts](src/lib/notification-link.ts) thêm `bank`/`bank_account`/`bank_submitted` → `/admin/bank-accounts` (admin) hoặc `/host/settings/bank` (host). Typecheck **0 errors**, lint **0 warning mới**. **Lưu ý BE**: dữ liệu cũ trước v1.21 được backfill `bankStatus='approved'` (VietQR chạy tiếp, không cần gửi lại); chỉ lần sửa tiếp theo mới đi qua luồng duyệt. |
| 2026-07-05 | tele-bot | **Bot Telegram thông báo hệ thống** — service Node.js độc lập [scripts/tele-bot/](scripts/tele-bot/) (0 dependency, native fetch, ngoài Clean Arch của app), chạy `npm run tele-bot`. Login BE bằng tài khoản riêng (⚠️ BE giới hạn 1 phiên web/tài khoản §1.6.1 — dùng chung account admin đang mở web sẽ đá phiên nhau), poll 6 nguồn mỗi 30s: `/notifications` (toàn bộ), `/bookings` (đơn mới + đổi trạng thái), `/admin/disputes`, `/admin/kyc/queue?filter=pending`, `/admin/payments?status=pending`, `/admin/subscriptions/count-overdue` → diff với `state.json` (gitignored) → gửi Telegram sendMessage (HTML). Lần chạy đầu seed im lặng không dội lịch sử; access token tự refresh, refresh bị đá (403) → re-login; tài khoản không ADMIN → 4 watcher admin tự tắt; Telegram 429 → chờ `retry_after`; cap `MAX_MESSAGES_PER_CYCLE=25`/chu kỳ. Config qua `scripts/tele-bot/.env` (xem [.env.example](scripts/tele-bot/.env.example) + [README.md](scripts/tele-bot/README.md)). |
| 2026-07-04 | booking-total-estimate | **Trang chi tiết booking host hiện "Tổng ≈ (tạm tính)" khi BE chưa chốt giá.** BE trả `totalAmount=null` cho đơn HOLD/CONFIRMED (spec §5.3 — chỉ ghi khi mark-paid) ⇒ ô "Tổng"/"Còn lại" trước đó chỉ hiện "Chưa chốt giá". Fix: helper mới `estimateBookingTotal()` trong [booking-display.ts](src/lib/booking-display.ts) — cộng giá từng đêm [checkIn, checkOut) từ bảng giá cơ sở (`weekdayPrice`/`weekendPrice`, đêm T6/T7/CN dùng giá weekend — cùng quy ước dow 0/5/6 với [lib/pricing.ts](src/lib/pricing.ts); không tính giá lễ/phụ thu, trả `null` khi thiếu bảng giá/ngày sai, cap 366 đêm). [host/bookings/[id]/page.tsx](src/app/(host)/host/bookings/[id]/page.tsx): khi `totalPrice==null` && status hold/confirmed → fetch `getPropertyAction(propertyId)` → hiện "≈ X ₫" + hint "Tạm tính theo bảng giá — chốt khi ghi nhận thanh toán" (cột "Còn lại" cũng ≈); `BookingActions` nhận `effectiveTotal` để form ghi nhận thanh toán gợi ý đúng số thay vì 0. Đơn có `totalAmount` thật hiển thị như cũ, không đổi gì phía admin. Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-04 | topbar-dropdown-ux + notification-deeplink | **(1) Dropdown topbar (menu tài khoản + chuông thông báo) đóng khi click ra ngoài / Esc.** Menu tài khoản tách khỏi `<details>` thành client component mới [user-menu.tsx](src/components/layout/user-menu.tsx) (click toggle). **Root cause backdrop cũ không hoạt động**: topbar có `backdrop-blur` → `backdrop-filter` trên cha biến nó thành containing block của con `position:fixed` ⇒ backdrop `fixed inset-0` chỉ phủ đúng thanh topbar, click bên dưới không trúng. Fix chung cho cả [user-menu.tsx](src/components/layout/user-menu.tsx) + [notification-bell.tsx](src/components/host/notification-bell.tsx): bỏ backdrop, dùng listener `pointerdown` trên `document` (đóng khi click ngoài `rootRef`) + phím Esc; listener chỉ gắn khi menu mở. **(2) Click notification điều hướng thẳng tới đối tượng** (đơn đặt phòng, hội thoại chat…). Root cause: BE trả `NotificationDto` spec §8.2 (`targetType`/`targetId`/`subtitle`/`isRead`/`type:number`) nhưng `ApiNotificationRepository` cast thẳng sang entity FE (`link`/`body`/`readAt`) ⇒ `link` luôn null (không điều hướng) + `readAt` luôn undefined (mọi tin hiện chấm chưa đọc). Fix: entity [notification.ts](src/core/entities/notification.ts) thêm `targetType`/`targetId`; repo [api-notification-repository.ts](src/infrastructure/repositories/api-notification-repository.ts) thêm `mapNotification()` tolerant (đọc cả 2 tên field, `isRead:true → readAt=createdAt`, type numeric 0/1/2 → 'booking'/'payment'/'system'); helper mới [lib/notification-link.ts](src/lib/notification-link.ts) `notificationHref(n, area)` suy deep-link từ `targetType` (booking→`/{area}/bookings/:id`, conversation/message/chat→`/host/messages/:id`, property/lead/kyc/dispute/review/user/payment/subscription; ưu tiên `n.link` BE trả sẵn; không biết → `null` = chỉ mark-read). [notification-bell.tsx](src/components/host/notification-bell.tsx) nhận prop `area: 'admin'\|'host'` (topbar truyền theo role) để deep-link đúng khu vực. **Known gap**: link "Xem tất cả thông báo →" trỏ `/host/notifications` — trang CHƯA tồn tại (404), cần làm ở đợt sau. Typecheck **0 errors**, lint 0 warning mới. |
| 2026-07-04 | system-sale-module | **Module mới "Tài khoản Sale hệ thống" (spec §26 v1.15, chỉ ADMIN) — tạo/mời, cấp quyền, vô hiệu hoá System SALE.** (1) **Permission mở rộng 4→18 module** [permission.ts](src/core/entities/permission.ts): giữ `PERMISSION_MODULES` (4 owner-scope) + mới `ADMIN_PERMISSION_MODULES` (14 admin-scope: users/kyc/subscriptions/payments/disputes/reviewsModeration/propertiesModeration/audit/leads/support/emails/billing/appVersion/dashboard) + `ALL_PERMISSION_MODULES`, `PermissionScope`, `modulesForScope()`, label tiếng Việt đủ 18; `PermissionMatrix.scope?`. [api-permission-repository.ts](src/infrastructure/repositories/api-permission-repository.ts) parse response §26.5 `{user:{scope}, permissions}` (tolerant shape cũ), fill default theo scope; Zod [actions/permissions.ts](src/app/actions/permissions.ts) nhận đủ 18 module (BE validate theo scope target). (2) **Full stack System SALE**: entity [system-sale.ts](src/core/entities/system-sale.ts), port [system-staff-repository.ts](src/application/ports/system-staff-repository.ts) (list/create/update/remove), use case [application/system-staff/actions.ts](src/application/system-staff/actions.ts) (Zod: name≥2, email, phone `0\d{9,10}` optional, password ≥8 có chữ+số), repo [api-system-staff-repository.ts](src/infrastructure/repositories/api-system-staff-repository.ts) (list **`GET /users?scope=system`** §26.3.2 — BE ép role=2, FE lọc phòng thủ `role===2 && scope==='system'`, response KHÔNG hydrate permissions[]; tạo trực tiếp `POST /users {role:2,scope:'system'}` §26.4; xoá **`DELETE /users/:id`** spec §3), container `systemStaffRepository()`, 4 Server Action [actions/system-staff.ts](src/app/actions/system-staff.ts) đều `requireAdmin`. **Luồng invite email KHÔNG dùng** (user bỏ 2026-07-04 — chỉ tạo trực tiếp; BE vẫn có `/admin/system-staff/invites` nếu cần khôi phục). (3) **UI**: trang mới [/admin/system-staff](src/app/(admin)/admin/system-staff/page.tsx) — bảng tài khoản (liên hệ, trạng thái active/disabled, ngày tạo, hành động **Sửa + Xoá**); [system-sale-dialogs.tsx](src/components/admin/system-sale-dialogs.tsx) dialog tạo trực tiếp (fieldErrors inline; tạo xong chỉ toast thành công + đóng dialog — user bỏ màn CTA "Cấp quyền ngay"); [system-sale-row-actions.tsx](src/components/admin/system-sale-row-actions.tsx) Sửa + Xoá (ConfirmDialog "không thể hoàn tác"). (4) **PermissionEditor scope-aware** [permission-editor.tsx](src/components/admin/permission-editor.tsx): prop `modules`+`hint`; trang [/admin/permissions](src/app/(admin)/admin/permissions/page.tsx) render 18 module khi `matrix.scope==='system'` (4 module owner như cũ), bảng chọn SALE thêm badge "Sale hệ thống". (5) **Sidebar** [manager-sidebar.tsx](src/components/layout/manager-sidebar.tsx): mục "Tài khoản Sale hệ thống" (UserCog) nhóm Quản lý nền tảng, giữa "Người dùng" và "Toàn bộ cơ sở". (6) **AdminUser.scope wire hoàn chỉnh** [api-admin-user-repository.ts](src/infrastructure/repositories/api-admin-user-repository.ts): map `scope` từ response (fix lỗi typecheck do entity sửa dở trước đó) + truyền query `?scope=` server-side (§26.3.2). (7) **Sửa tài khoản**: `SystemSaleEditDialog` (form giống form tạo, prefill name/email/phone + ô "Mật khẩu mới" để trống nếu không đổi) → `updateSystemSaleAction` → use case gọi `PUT /users/:id` (admin sửa được anyone, spec §3) — BE nhận `password` ngay trong body nên đổi mật khẩu cùng 1 request (không cần reset-password riêng). (8) **Chốt UI theo yêu cầu user**: cột Hành động = **Sửa + Xoá** (`DELETE /users/:id`, hard delete, ConfirmDialog cảnh báo); bỏ cột "Quyền" + bỏ nút/section "Mời qua email" (phân quyền vào qua sidebar Phân quyền hoặc CTA sau khi tạo). (9) **Bộ lọc**: mới [system-sales-browser.tsx](src/components/admin/system-sales-browser.tsx) (client) — tab trạng thái Tất cả/Đang hoạt động/Tạm khóa kèm count + ô tìm kiếm tên/email/SĐT, lọc **in-memory** trên list fetch 1 lần (0 round-trip khi gõ), URL sync `?status=&q=` bằng `history.replaceState` (deep-link/refresh giữ bộ lọc, không re-run server — pattern giống properties-browser). (10) **Dropdown trạng thái trong form Sửa**: select "Hoạt động/Tạm khóa" → `isActive: boolean` gửi trong cùng `PUT /users/:id` (UpdateSchema thêm `isActive: z.boolean()`); nhãn badge/tab đổi "Đã vô hiệu hoá" → "Tạm khóa" (amber). Repo `create` nới `createdAt` optional (`POST /users` không trả field này — list có). Typecheck **0 errors**, lint **0 warning mới**. |
| 2026-06-29 | tối ưu perf trang Duyệt cơ sở | **Tối ưu performance `/admin/properties`: fetch 1 lần → lọc/tìm/sort/phân trang client-side.** Trước: mỗi lần đổi tab / gõ tìm kiếm / đổi sort / đổi trang đều push URL param ⇒ Server Component chạy lại ⇒ `listPropertiesAction` fetch lại **toàn bộ** cơ sở từ BE (lãng phí, mỗi keystroke search = 1 lần đổ cả list). **Không dùng Next Data Cache để vá** được: `GET /properties` cùng URL nhưng BE trả data khác theo token (admin vs từng owner đều gọi `?includeInactive=true`) ⇒ cache theo URL sẽ rò rỉ chéo người dùng. **Giải pháp**: (1) Server [page.tsx](src/app/(admin)/admin/properties/page.tsx) chỉ fetch **1 lần**, map `Property` → **`AdminPropertyRow` rút gọn** (`toRow()` bỏ amenities/policies/description… giảm payload serialize xuống client), parse `searchParams` thành `initial` state rồi render Client Component. (2) **Mới** [properties-browser.tsx](src/components/admin/properties-browser.tsx) (client orchestrator): giữ state status/q/type/sort/page, `useMemo` lọc+sort+đếm count+phân trang **in-memory** ⇒ mọi thao tác 0 round-trip. Đồng bộ URL bằng **History API `replaceState`** (không `router`) ⇒ deep-link (dashboard → `?status=pending`), refresh, share vẫn đúng mà KHÔNG re-run server. (3) **Mới** [admin-property-card.tsx](src/components/admin/admin-property-card.tsx) (`AdminPropertyRow` type + card presentational). (4) **Mới** [client-pagination.tsx](src/components/ui/client-pagination.tsx): phân trang bằng `onClick`+state thay vì `<Link href>` (link sẽ navigate → re-fetch). (5) [property-filters.tsx](src/components/admin/property-filters.tsx) refactor sang **controlled** (callbacks `onStatusChange/onSearchChange/…` thay vì tự `router.replace`); debounce search 350→250ms, bỏ spinner pending (lọc tức thì). Kết quả: tải trang 1 request; đổi tab/tìm/sort/trang **tức thì, không gọi BE**. Sau khi approve/reject/suspend ở trang chi tiết → `revalidatePath('/admin/properties')` đảm bảo lần vào lại fetch mới. Typecheck **0 errors**, lint **0 warning mới**. |
| 2026-06-29 | khôi phục luồng duyệt cơ sở | **🔴 BREAKING business — Đảo ngược quyết định 2026-06-13 ("bỏ duyệt cơ sở"). Khôi phục luồng duyệt 4-trạng thái** (spec §4.4/§4.5/§4.10, BE đã có endpoint). (1) **Entity** [property.ts](src/core/entities/property.ts): thêm `ModerationStatus = 'pending'\|'approved'\|'rejected'\|'suspended'` + field `moderationStatus`, `moderationRejectedReason`, `moderationReviewedAt`, `isHot` (BE trả cùng property DTO, không cần mapper vì `apiClient.get` cast thẳng). (2) **Helper mới** [lib/property-moderation.ts](src/lib/property-moderation.ts): `MODERATION_STATUS_LABEL/ICON/VARIANT` + `moderationStatusLabel()` (dùng chung admin + host, tránh drift). (3) **Port + repo**: thêm `setHot(id, isHot)` → `PATCH /properties/:id/hot`; `approve/reject/suspend` đã sẵn. (4) **Use case mới** [application/properties/moderate.ts](src/application/properties/moderate.ts): approve/reject/suspend/setHot. (5) **Actions** [admin.ts](src/app/actions/admin.ts) viết lại: `approvePropertyAction` (pending/rejected/suspended → approved), `rejectPropertyAction` (Zod reason ≥5 ≤500), `suspendPropertyAction(id, reason?)` (đổi chữ ký từ `(id, boolean)` cũ — giờ POST `/suspend` thay vì PATCH isActive), `setPropertyHotAction`. Mọi action `requireAdmin` + uuid-validate + revalidate. (6) **UI** [approval-actions.tsx](src/components/admin/approval-actions.tsx) viết lại: nút theo từng trạng thái + modal nhập lý do (reject bắt buộc, suspend tuỳ chọn) qua Dialog primitive. **Mới** [hot-toggle.tsx](src/components/admin/hot-toggle.tsx). (7) [admin/properties/page.tsx](src/app/(admin)/admin/properties/page.tsx): 5 stat-tile + 4-tab lọc theo `moderationStatus` (client-side trên `?includeInactive=true` — BE chưa có query `?moderationStatus=`), badge trạng thái + badge Hot. (8) [property-filters.tsx](src/components/admin/property-filters.tsx): highlight count pending (amber) + rejected/suspended (rose). (9) [admin/properties/[id]/page.tsx](src/app/(admin)/admin/properties/[id]/page.tsx): badge trạng thái + banner lý do từ chối + ApprovalActions(moderationStatus) + HotToggle. (10) [admin/page.tsx](src/app/(admin)/admin/page.tsx): `pendingProperties` dùng `moderationStatus==='pending'` (bỏ hack `!isActive && bookingCount===0`). (11) [host/properties/page.tsx](src/app/(host)/host/properties/page.tsx): OWNER thấy badge pending/rejected/suspended + lý do từ chối. **BE đã chốt (2026-06-29)**: giữ **auto-approve khi tạo phòng** (không dựng queue duyệt mới ⇒ tab "Chờ duyệt" rỗng ~99%, để cuối); action chính là `reject`/`suspend`, `approve` chỉ để gỡ rejected/suspended về approved; BE đã thêm query `GET /properties?moderationStatus=approved\|rejected\|suspended\|pending&includeInactive=true`. **FE align**: thứ tự tab/tile = Tất cả → Đã duyệt → Đã từ chối → Đã tạm ngưng → Chờ duyệt; nhãn rejected/suspended = "Đã từ chối"/"Đã tạm ngưng". **Giữ lọc client-side** trên 1 request `?includeInactive=true` (đổ tất cả) để render count-badge từng tab trong 1 lần gọi — query `?moderationStatus=` để dành khi chuyển sang phân trang server-side. Web khách hàng không đổi gì. Typecheck **0 errors**, lint **0 warning mới**. |
| 2026-05-08 | init | Khởi tạo CLAUDE.md, áp dụng Clean Architecture, tích hợp API Auth/Properties/Dashboard, mock cho phần còn lại |
| 2026-05-08 | wire-up | Wire layouts `(admin)`/`(host)` dùng `getCurrentProfile()` + role guard. Wire `/host` dashboard và `/host/properties` qua Server Actions mới. Thêm mocks cho Payments/Disputes/Calendar (entities + ports + repos + use cases + actions). Loại bỏ Supabase foundation: `lib/supabase/*` thành stubs deprecated, `middleware.ts` no-op, `auth/callback` & `auth/signout` chuyển sang Clean Arch. `isSupabaseConfigured()` luôn `false` ⇒ 41 file legacy có guard `if (!isSupabaseConfigured()) return DEMO` tự động chạy demo mode. |
| 2026-05-08 | api-fix | Fix base URL: BE thực tế chạy không có prefix `/api/v1`. Đổi `NEXT_PUBLIC_API_BASE_URL=http://api.halong24h.com` (verify: `POST /auth/login` → 401, `POST /api/v1/auth/login` → 404). |
| 2026-05-08 | supabase-removal | **Loại bỏ Supabase hoàn toàn.** Refactor 12 action files (`actions/property.ts`/`booking.ts`/`dispute.ts`/`tenant.ts`/`room.ts`/`lead.ts`/`pricing.ts`/`chat.ts`/`storage.ts`/`notification.ts`/`admin.ts`/`room-block.ts`) chỉ giữ Zod validation + return mock-success. 29 page files & components: strip Supabase imports + dead code blocks bằng Node script (brace matcher). Convert `image-uploader.tsx` sang gọi `uploadPropertyImagesAction` thay Supabase Storage. Xoá `src/lib/supabase/`, uninstall `@supabase/ssr` + `@supabase/supabase-js`. Giữ `src/lib/database.types.ts` làm legacy domain types (string-literal unions, không phụ thuộc DB). Typecheck: 0 lỗi mới — chỉ 5 lỗi pre-existing (component thiếu sẵn từ trước). |
| 2026-05-08 | missing-components | Tạo 4 component thiếu pre-existing: `components/listing/property-card.tsx` (export `PropertyCardData`), `components/property/amenity-list.tsx`, `components/account/profile-form.tsx`, `components/account/password-form.tsx`. Thêm `changePasswordAction` Server Action wrapping `POST /auth/change-password`. Typecheck **0 errors**. |
| 2026-05-08 | api-error-ux | HTTP client log lỗi với context `[api-client] METHOD /path → STATUS message`. Thêm `humanize()` translate BE messages thành tiếng Việt (Validation failed, Invalid token, Phone already registered, etc.). |
| 2026-05-08 | image-hosts | Thêm `res.cloudinary.com`, `*.googleusercontent.com`, `img.vietqr.io` vào `next.config.ts` `remotePatterns` (BE host ảnh property qua Cloudinary). |
| 2026-05-08 | api-expansion | BE thêm endpoint thật: `GET /bookings` + `/notifications` + `/notifications/unread-count`. Wire `ApiBookingRepository`, tạo `ApiNotificationRepository` + entity + port + use case + Server Action. Switch default mode booking từ `mock` sang `api` trong container. Refactor 5 page chính sang Clean Arch: `/host/bookings` (list), `/host/properties/[id]` (detail dùng `getPropertyAction` + adapter sang legacy form shape), `/admin` (dashboard dùng API stats + bookings), `/admin/properties` (list dùng `listPropertiesAction`), `/admin/bookings` (list dùng `listBookingsAction`). Pages còn lại (calendar, leads, messages, hk, guests, staff, settings, reports) vẫn render demo qua mock — sẽ migrate khi BE bổ sung endpoint. |
| 2026-05-16 | audit-self-write | Audit log không còn demo tĩnh. Tạo `core/entities/audit-log.ts` + `application/ports/audit-log-repository.ts` + `application/audit-log/actions.ts` + `infrastructure/mocks/mock-audit-log-repository.ts` (singleton in-memory store, seeded với 5 entry). Helper `lib/audit-recorder.ts` ghi best-effort từ Server Action (không throw nếu fail). Wire vào 12 admin action: `approveKycAdminAction`, `rejectKycAdminAction`, `banAdminUserAction`, `unbanAdminUserAction`, `revokeUserSessionAction`, `updateUserSubscriptionAction`, `resetUserPasswordAction`, `approvePropertyAction`, `rejectPropertyAction`, `suspendPropertyAction`, `startDisputeInvestigationAction`, `resolveDisputeAction`, `rejectDisputeAction`. Page `/admin/audit-log` dùng real repo + filter theo loại hành động / đối tượng / search query. |
| 2026-06-10 | users-search-q | BE thêm query `q` cho `GET /users` (search name/phone/email, case-insensitive, BE tự trim + max 100 ký tự, kết hợp được với `role`/`withStats`). Fix `ApiAdminUserRepository.list`: query key đổi từ `search` → `q` (BE bỏ qua param `search` cũ ⇒ ô tìm kiếm `/admin/users` trước đó không lọc gì khi chạy live API). UI/action/use-case/page đã wire sẵn `q` từ trước — không đổi. Trim/cap do BE xử lý, FE không lặp lại. Typecheck **0 errors**. |
| 2026-05-17 | reviews-emails-subscription | (1) Reviews moderation: `core/entities/review.ts` + port + use case + mock (5 seed: 1 spam có flag SĐT cạnh tranh, 1 profanity đã ẩn, 3 normal) + page `/admin/reviews` + filter status/rating/flagged/search + `ReviewModerationActions` (hide với reason ≥5, restore) + sidebar entry. (2) Test send email: Server Action `sendTestEmailAction` (mock mode log console, live mode chờ BE `POST /admin/emails/test`) + `TestEmailButton` thay nút disabled, pre-fill admin email. (3) Subscription tracking: `core/entities/subscription.ts` (4 plan, 4 status: paid/pending/overdue/frozen, expireAt + auto-mark overdue) + port + use case + mock (5 seed bao gồm 1 frozen + 1 overdue) + actions (`listSubscriptionsAction`, `getMySubscriptionAction`, `markSubscriptionPaidAction`, `freezeSubscriptionAction`, `unfreezeSubscriptionAction`) + helper `lib/subscription-guard.ts` `blockedReason()` + refactor `/admin/payments` dùng real repo + `SubscriptionRowActions` (mark paid / freeze / unfreeze) + `/host/settings/subscription` cho owner xem & STK chuyển khoản + banner ở host layout + guard chặn tạo property/booking khi overdue/frozen. |
| 2026-06-05 | spec-v1.7-phase-17a | **Phase 17a — Google Sign-In + xoá Apple Sign-In**. (1) **Xoá Apple sign-in**: `appleProfile` khỏi `OAuthNewUserPrompt`, `googleProfile` thành required. (2) **`<GoogleSignInButton>`** ([133 dòng](src/components/auth/google-sign-in-button.tsx)): Google Identity Services qua `<Script>` next/script — KHÔNG thêm npm dep. Render official button, callback nhận JWT credential. Type `window.google.accounts.id`. Fallback hint khi thiếu `NEXT_PUBLIC_GOOGLE_CLIENT_ID`. (3) **`<RoleSelectDialog>`** ([83 dòng](src/components/auth/role-select-dialog.tsx)): modal hiện khi BE trả `isNewUser: true`. 2 card OWNER/CUSTOMER với icon + hint + warning Customer không vào trang quản lý. (4) **`<GoogleLoginSection>`** ([66 dòng](src/components/auth/google-login-section.tsx)): orchestrator, state busy/error/prompt, lưu idToken để gọi lại action kèm role. (5) **`loginWithGoogleAction`**: discriminated union return `{kind:'redirect'\|'needs-role'}`. Write tokens → getProfile → CUSTOMER clearTokens. (6) **Wire login page**: Google section tách ra ngoài form email/password. (7) **env.local.example**: `NEXT_PUBLIC_GOOGLE_CLIENT_ID` + hướng dẫn lấy Web Client ID. **Bug fix tự rà**: `onCredential` prop reference unstable gây button trùng lặp — fix với `useRef<callback>` + `parent.innerHTML=''` clear trước renderButton. Typecheck **0**, lint **0**. **Files mới**: 3. **User cần làm**: tạo Web Client ID Google Cloud Console + báo BE Client ID. |
| 2026-05-17 | dev-role-switch | `actions/auth.ts` mở rộng dev bypass: 7 profile (admin/owner/owner-nokyc/owner-overdue/sale/sale-noown/customer), switch bằng cookie `dev_role` (không cần restart server). Cho phép giả lập đầy đủ 4 vai trong test. |
| 2026-06-05 | spec-v1.7-phase-16 | **Phase 16 — Spec v1.4-v1.7 catch-up (BREAKING auth + payment scope + manual reconcile)**. (1) **🔴 CRITICAL Phase 16a — Auth response BREAKING v1.7**: Spec §2.3 tách auth response và profile. `/auth/login`, `/auth/register`, `/auth/refresh`, `/auth/google`, `/auth/apple`, `/staff/invites/accept` không còn trả `user` object — chỉ `{ accessToken, refreshToken }`. FE phải gọi `GET /auth/profile` riêng sau auth. **Changes**: thêm `AuthTokens` type, `OAuthSignInResult = AuthTokens \| OAuthNewUserPrompt`, type guard `isOAuthNewUserPrompt`. Xoá `AuthSession` (deprecated). AuthRepository.{login,register,refresh,acceptInvite} return `AuthTokens`. `loginAction` thêm step: write tokens → call `getProfile()` → check role → clearTokens nếu CUSTOMER. `signupAction` cùng pattern best-effort getProfile. Affected: 6 file (entity/port/use-case/api+mock staff repo/api auth repo/action). (2) **Phase 16b — Payment method `bank_transfer` only** (spec v1.4): Local Payment entity đã xoá Phase 11a. legacy-types.ts có `PaymentMethod = cash|bank_transfer|vietqr|momo` — không dùng trong app code (chỉ type stub). No-op cleanup. (3) **Phase 16c — SubscriptionProvider sync** (spec v1.4 + v1.6): thêm `casso`, `sepay` vào enum (giữ `apple_iap` per user intent). Update 2 file: `user.ts` + `subscription.ts`. (4) **Phase 16d — Wire `/admin/payments` v1.6 manual reconcile**: full new module. Entity [payment-session.ts](src/core/entities/payment-session.ts) với 5-state lifecycle (`pending\|paid\|expired\|failed\|refunded`), TTL 24h, ckContent format `HALONG24H <sessionId>`. Port + ApiRepo (`GET /admin/payments` + `POST /admin/payments/:id/mark-paid`). Server Actions [payment-sessions.ts](src/app/actions/payment-sessions.ts) (list+markPaid với Zod). New page [/admin/payments/sessions](src/app/(admin)/admin/payments/sessions/page.tsx) (215 dòng): tabs status filter + search + table + banner TK ACB 21169431 NGUYEN VU NAM + workflow guide. `<MarkSessionPaidButton>` ([130 dòng](src/components/admin/mark-session-paid-button.tsx)): button → Dialog form nhập reference banking (optional, 64 chars max) → action → toast + router.refresh. Link từ trang `/admin/payments` chính qua nút "💸 Đối soát thủ công". Container thêm `paymentSessionRepository()`. (5) **Phase 16e — `/host/settings/subscription` UI check**: grep tìm method dropdown/select — không tìm thấy. Page đã clean trước, no-op. Typecheck **0 errors**, lint **0 warnings** (Phase 16 files), no silent catches. **Files mới**: 5 (entity+port+repo+action+page+component). **Files edit**: 9 (entity user/sub + 2 port + use case + 3 staff repo + container + 2 page). |
| 2026-06-05 | spec-v1.5-phase-15a | **Phase 15a — Chat attachment upload** (spec v1.5 §23 BE đã ra `POST /uploads` + `DELETE /uploads/:id`). (1) **Entity + constants** ([core/entities/upload.ts](src/core/entities/upload.ts)): `UploadResult` shape khớp spec, `UPLOAD_ALLOWED_MIME` whitelist (image/jpeg/png/webp/gif + pdf), `UPLOAD_MAX_SIZE_BYTES=10MB`, `CHAT_ATTACHMENT_MAX_COUNT=5` (spec §17.5), helpers `isAllowedMime`/`isImageMime`. (2) **Server Actions** ([app/actions/uploads.ts](src/app/actions/uploads.ts)): `uploadFileAction(FormData)` validate size + mime trước rồi relay multipart sang `POST /uploads` (BE double-check magic bytes); `deleteUploadAction(id)` cho user cancel pending. (3) **`<AttachmentPicker>`** ([components/chat/attachment-picker.tsx](src/components/chat/attachment-picker.tsx) — 199 dòng): paperclip button + hidden file input multiple + accept attr theo whitelist. Upload tuần tự (tránh spike rate-limit). Preview ObjectURL ngay cho image, FileText icon cho PDF. Remove khả dụng: revoke ObjectURL + DELETE /uploads/:id best-effort (orphan cron BE dọn 24h nếu fail). Disabled khi đạt max=5. Inline error display. (4) **Wire vào ChatThread**: state `pendingAttachments`, `handleSend` snapshot + send với `attachments[]`, optimistic message kèm attachments, rollback restore khi fail (cho user retry). Cleanup ObjectURLs khi unmount + sau gửi thành công. Form submit enabled nếu có content HOẶC attachments. (5) **Render trong MessageBubble** ([message-bubble.tsx](src/components/chat/message-bubble.tsx) — 299 dòng): `<AttachmentList>` component nội bộ — image inline (Next/Image với `unoptimized` cho Cloudinary URL chưa qua optimizer), PDF/file link với icon + size + Download icon. Style tone-matched fromMe (white/10) vs other (cream-100). (6) **SendSchema fix** ([conversations.ts](src/app/actions/conversations.ts)): `content` min(1) → max(5000), thêm Zod `refine` rule `content || attachments.length > 0`. Cho phép gửi tin chỉ có attachment. (7) **Extract `<ChatStatusBar>`** ([status-bar.tsx](src/components/chat/status-bar.tsx) — 46 dòng): kéo indicator bar (connected/peer online/typing/error) ra component riêng → chat-thread 431→399 dòng (về dưới 400). (8) **Cleanup**: bỏ unused `cn` import + 1 unused eslint-disable + condense comments. **6 bug review** all OK: optimistic flow đúng, ObjectURL cleanup chuẩn, race-upload-while-send acceptable (chỉ gửi completed), partial sequential upload acceptable, BE error inline. Typecheck **0 errors**, lint **0 warnings**, no silent catches. **Tổng dòng Phase 15a**: 943 (5 file mới + 2 file edit). |
| 2026-06-05 | spec-v1.3-phase-14 | **Phase 14 — UX polish + accessibility**. (1) **`<ConfirmDialog>` component mới** ([components/ui/confirm-dialog.tsx](src/components/ui/confirm-dialog.tsx) — 68 dòng): reuse component dùng RDialog (Radix) + Framer Motion (đã có). Props: `open/title/description/confirmLabel/cancelLabel/variant:danger\|primary/pending`. Tự handle `onOpenChange` không cho close khi pending. (2) **Replace 5 `confirm()` native bằng ConfirmDialog**: [MessageBubble](src/components/chat/message-bubble.tsx) (xoá tin), [ImageGrid](src/components/host/image-grid.tsx) (xoá ảnh property), [UserModerationActions](src/components/admin/user-moderation-actions.tsx) (4 calls: unban/revoke/changePlan/resetPwd). UserModeration dùng discriminated union state `PendingConfirm` để track action nào đang được confirm — tránh 4 dialog component riêng. (3) **Enter để gửi tin** ([chat-thread.tsx](src/components/chat/chat-thread.tsx)): `handleTextKeyDown` — Enter submit form, Shift+Enter xuống dòng. Check `e.nativeEvent.isComposing` cho IME (Vietnamese typing) tránh trigger sai khi composing. Placeholder update gợi ý phím tắt. (4) **Scroll-to-bottom button**: `atBottom` state track qua `handleScroll` (cách bottom <100px). Auto-scroll khi tin mới chỉ trigger khi user đang ở bottom (không kéo người đang đọc tin cũ xuống). Khi không atBottom → hiện nút FAB tròn `ArrowDown` ở góc phải. Click → smooth scroll. (5) **Layout fix**: wrap ChatThread output trong `<div className="relative flex flex-col flex-1 min-h-0">` để scroll button absolute đúng vị trí (trước đó positioned vs viewport). Typecheck **0 errors**, lint **0 warnings**, no silent catches. **File sizes**: chat-thread 331→379, message-bubble 191→203, confirm-dialog 68 (all <400). |
| 2026-06-05 | spec-v1.3-phase-13 | **Phase 13 — Chat UX enhancements (typing/presence/edit/delete)**. (1) **Typing indicator** ([chat-thread.tsx](src/components/chat/chat-thread.tsx)): `handleTextChange` throttle emit `typing:start` mỗi 3000ms theo spec §17.6 FE checklist + auto `typing:stop` sau 4000ms không gõ. Hub state `typingUsers: Set<string>` update từ `onTyping` (skip self). Cleanup timer + emit stop khi unmount. UI hiển thị "Đang nhập tin nhắn..." italic ở indicator bar. (2) **Presence**: state `onlineUsers: Set<string>` từ `onPresence` event. ChatThread thêm prop `peerUserId?: string`, page truyền `customerOf(conv)?.userId`. Render badge "● Đang online" emerald khi peer online. (3) **Edit/Delete UI** ([message-bubble.tsx](src/components/chat/message-bubble.tsx) — component mới 191 dòng): extract khỏi ChatThread cho clean separation. Edit window 15 phút theo spec §17.1. Logic: hover hiện 2 nút "Sửa"/"Xoá" trên góc bubble (chỉ với own messages, không deleted, không optimistic, trong 15-min window cho Edit). Edit mode inline textarea với Ctrl+Enter submit, Esc cancel. Delete với `confirm()` native. Optimistic UI: callback `onEdited`/`onDeleted` cập nhật ChatThread state. WS broadcast trùng setState với content giống → no-op render. (4) **Bug fix tự rà 4 điểm**: (a) typing emit khi disconnected — `handleTextChange` early return `!socket\|\|!connected`; (b) onTyping/onPresence skip self event; (c) edit-window expire mid-render — chấp nhận stale UI minor, BE sẽ reject "out of window"; (d) edit/delete race với WS broadcast — setState identical content = no-op acceptable. (5) **Refactor**: bỏ inline message render trong ChatThread, thay bằng `<MessageBubble>`. Loại `formatDateTime` import unused. File chính 263→331 lines (vẫn <400). Typecheck **0 errors**, lint **0 warnings**. **Known limitation**: `confirm()` browser native — TODO Phase 14 thay bằng modal component. |
| 2026-06-05 | spec-v1.3-phase-12 | **Phase 12 — Chat detail page với full WebSocket integration**. (1) **`<ChatThread>` Client Component mới** ([components/chat/chat-thread.tsx](src/components/chat/chat-thread.tsx)): tích hợp `useChatSocket` hook cho realtime + REST cho hold pagination. Render message list, composer với optimistic UI, indicator "Trực tuyến" (connected) + connection error, nút "↑ Tin cũ hơn" với cursor pagination. (2) **Server Component shell** ([page.tsx](src/app/(host)/host/messages/[id]/page.tsx)): fetch song song qua Promise.all 4 thứ — `getConversationAction`, `listConversationsAction`, `getCurrentProfile`, `readTokens`. Truyền `accessToken` xuống ChatThread cho Socket.IO `auth.token`. Redirect login nếu thiếu profile/token. Sidebar list conversations với unread badge. (3) **Bug fix tự rà (4 bug)**: (a) **mark-read spam loop** — useEffect deps `[messages.length]` gọi mark-read mỗi tin gửi → fix: tách 2 effect, mount-once + lastFromOther-only (last message từ người khác); (b) **race condition WS+REST optimistic** — sender nhận lại tin mình gửi qua cả WS lẫn REST → duplicate; fix `onMessage` skip khi `senderId === currentUserId` (REST response authoritative); (c) **auto-scroll khi loadOlder** — prepend tin cũ → scroll xuống cuối (sai UX); fix: `lastAppendRef` track action, chỉ scroll khi append; (d) **optimistic id collision** — `local-${Date.now()}` không unique nếu 2 tin trong cùng ms; fix dùng `crypto.randomUUID()` fallback `Math.random()`. (4) **Cleanup orphan**: xoá `app/(host)/host/messages/[id]/message-composer.tsx` + `components/chat/message-composer.tsx` (cả 2 đã orphan, dùng action legacy) + `app/actions/chat.ts` (legacy, không có consumer). (5) **Security tradeoff documented**: accessToken expose vào client bundle vì WS cần. Acceptable: 15-min TTL + httpOnly cookie là primary auth. Comment trong Props. Typecheck **0 errors**, lint **0 warnings**. **Files xoá Phase 12**: 3 file legacy. **Components mới**: 1. |
| 2026-06-05 | spec-v1.3-phase-11 | **Phase 11 — Cleanup dead code + UI update**. (1) **Cleanup Payment dead module**: xoá 6 file (`core/entities/payment.ts`, `application/ports/payment-repository.ts`, `application/payments/list.ts` + folder, `infrastructure/mocks/mock-payment-repository.ts`, `app/actions/payments.ts`). Container bỏ `paymentRepository()` + import + type ref. Lý do: entity local (booking-level: bookingId/guestName/method:vietqr/cash/transfer) ≠ spec §10.2 (subscription-level: planId/cycle/method:vnpay_qr/bank_transfer/card). Action không có UI consumer. (2) **Skip cleanup Dispute extended fields**: `evidence/chatExcerpts/verdict/penalty` ĐANG được dùng UI (`/admin/disputes/page.tsx` show count badges + `/admin/disputes/[id]/page.tsx` render đầy đủ). BE Phase 10c trả `[]`/`null` an toàn → UI hiển thị blank section. Khi BE v2 trả data thật, UI tự fill. (3) **Permissions UI rewrite theo spec §12**: xoá `permission-matrix.tsx` cũ (hardcoded 5 group × N keys cho 3 role, có mock save setTimeout). Tạo mới `permission-editor.tsx` (Client Component) — bảng 4 module × 4 CRUD checkbox + nút "Bật/tắt hết" mỗi row + dirty banner + `useTransition` cho save (dùng `updatePermissionsAction` thật). Rewrite page `/admin/permissions/page.tsx`: nếu `?userId=` → load + show editor; không có → list SALE users (qua `listAdminUsersAction({ role: RoleCode.SALE })`) cho admin click chọn. Empty state khi chưa có SALE. (4) **Wire `/host/messages` list**: thay `getConversations() return DEMO_CONVERSATIONS` bằng `listConversationsAction()` + `getCurrentProfile()` (đồng bộ qua Promise.all). Adapter `adaptConversation(spec, currentUserId)` map `Conversation` spec §17.2 (`members[]`, `myUnread`, `lastSenderId`) → legacy demo shape (`customer_name`, `unread_owner`, `last_message_from_me`). 3-state pattern như leads page: `live`/`empty`/`demo-error`. Banner rose hiển thị error message khi fail. **Lưu ý** `property_name` BE không denormalize → dùng `subject` fallback, `online` = false (chờ presence WS). (5) **Wire `/host/leads/[id]` detail**: thay `getLead(id) return DEMO[id]` bằng `getLeadAction(id)`. Adapter `adaptLeadDetail(lead: Lead)` map entity camelCase → legacy snake_case (responded_at=contactedAt, property_id, etc.). Field `base_price/weekend_price/room_name` không có trong Lead entity (BE chưa hydrate) → undefined/null. DEMO[id] giữ làm fallback dev mode khi action fail. Typecheck **0 errors**. **Files xoá tổng (Phase 11)**: 7 file (5 Payment + 1 PermissionMatrix component + 1 application/payments/ folder). **UI page wired sang Clean Arch**: 4 page (`/admin/permissions`, `/host/messages`, `/host/leads`, `/host/leads/[id]`). |
| 2026-06-05 | spec-v1.3-phase-10 | **Phase 10 — Wire 3/4 module Mock còn lại sang API + security fix Phase 9 review**. (1) **SECURITY FIX** từ re-review: `token-storage.ts` đã bỏ `pendingTokens` module-level (cross-user leak risk khi serverless reuse module instance). Thay bằng pattern truyền `_accessTokenOverride` trực tiếp qua `RequestOptions` cho retry request. `writeTokens` giờ trả `boolean` (caller biết cookie ghi được không). `api-client.refreshTokens` giờ trả `{accessToken, refreshToken}` để retry dùng trực tiếp, không phụ thuộc cookie store. Refresh fail giờ log warn dev mode. (2) **Bug fix Booking mapper**: `isPaid` condition đổi từ `paidAt != null \|\| paidAmount >= totalAmount` → chỉ `paidAt != null`. Tránh edge case free booking (totalAmount=0) bị mark paid sai. (3) **Bug fix mock-booking update**: bỏ unsafe `as typeof b.deposit`, dùng `vnd(input.depositAmount)` đúng VND brand. (4) **ApiAdminUserRepository** (spec §3 + v1.3 §22 A2): `/users?withStats=true` map sang AdminUser entity. `propertyCount` + `bookingCount` từ BE, `disputeCount`+`lastActiveAt` defer v2 → default 0/null. `status` derive từ `bannedAt`+`isActive`. `subscriptionPlan` derive từ `subscriptionPlanId` prefix (`rooms_1\|5\|10\|30`). 6 endpoint: list+getById+ban+unban+revokeSession+resetPassword. `updateSubscription` qua `PATCH /admin/users/:id/subscription/price`. (5) **ApiKycAdminRepository** (spec §9.2 + v1.3 B5): 4-state confirmed. Local 8-state entity map xuống subset 4. `fields[]` 7-verification BE chưa expose → mảng rỗng (UI section blank đợi v2). 5 endpoint: list+getById+approve+reject+countPending. `reverseStatus` cho query: awaiting_approval/kyc_submitted → 'pending'. (6) **ApiDisputeRepository** (spec §13 + v1.3 B3): Type mapping `refund_request↔refund`, `service_quality↔quality`, `damage_claim↔fraud`. Status `pending↔open`. Extended fields (evidence/chatExcerpts/verdict/penalty) defer v2 → `[]` hoặc `null`. 7 endpoint: list+getById+countActive+open+startInvestigation+resolve+reject. `resolve` chỉ gửi `resolution + refundAmount` (extended penalty meta giữ FE-only). (7) **Skip ApiPaymentRepository**: local `Payment` entity (booking-level: bookingId/guestName/method:vietqr/cash) ≠ spec §10.2 `/payments/*` (subscription-level: planId/cycle/method:vnpay_qr). Local action `listPaymentsAction` không có UI consumer thật (admin/payments dùng subscription). **Dead code** — recommend Phase 11 dọn dẹp hoặc repurpose. (8) Container switch 3 repo sang default `api`, mock fallback qua env `NEXT_PUBLIC_DATA_MODE_{ADMIN_USERS\|KYC_ADMIN\|DISPUTES}=mock`. Typecheck **0 errors**. **Trạng thái mới**: 16/18 module wired API, 1/18 (subscriptions) opt-in API, 1/18 (payments) dead code. |
| 2026-06-05 | spec-v1.3-phase-9-fix | **Fix bug Phase 9 dựa trên BE response v1.3 + senior review**. (1) **CRITICAL Booking mapper** — `ApiBookingRepository` thêm `mapBooking(SpecBooking)` chuyển `customerName/customerPhone/checkinDate/checkoutDate/totalAmount/depositAmount/status:number/propertyName/nights` → entity `guestName/guestPhone/checkInAt/checkOutAt/totalPrice/deposit/status:string/propertyName/nights`. Numeric status map `0→hold, 1→confirmed, 2→cancelled, 3→completed`. Khi `paidAt != null \|\| paidAmount >= totalAmount` → upgrade status thành `paid`. `holdBody()` helper convert input lúc POST. Tất cả 10 method giờ qua mapper. (2) **B2 Calendar multi-property** — `ApiCalendarRepository.getGrid` join `propertyIds` thành CSV `?propertyIds=uuid1,uuid2` thay vì chỉ lấy `[0]`. (3) **B1 Review direct getById** — `ApiReviewRepository.getById` gọi thẳng `GET /admin/reviews/:id` thay vì list+find (bỏ N+1). (4) **A4 Subscription shape** — `mapSubscription` bỏ fallback chain `endsAt ?? nextChargeAt ?? expireAt ?? updatedAt`, chỉ dùng `nextChargeAt` (BE confirm không có `startsAt/endsAt`), bỏ legacy field. (5) **HIGH H1 token-storage** — write thất bại do RSC ctx giờ cache vào `pendingTokens` in-memory, `readTokens()` ưu tiên cache → retry request trong cùng request lifecycle dùng access token mới, không loop 401. (6) **H2 getCurrentProfile** — phân biệt 401/403 (login bình thường) vs 5xx/network (log warn dev mode). (7) **H3 fillDefaults permission** — warn dev mode khi BE trả thiếu module so với 4 module spec yêu cầu (raw.length > 0 nhưng < 4). (8) **H4 useChatSocket cleanup** — `disconnectChatSocket()` khi `token=null` (logout). Effect dependency đổi từ `[opts]` (object reference unstable) sang `[token, locale, baseUrl]` (primitive equality). (9) **H5 ApiSubscription** — cập nhật doc comment cho rõ `subscriptionId === userId` per BE confirm Option A. (10) **M1 container** — bỏ `void modeFor()` cho permissions+chat (env không có tác dụng → docstring chính xác). (11) **M2 middleware network** — phân biệt 5xx/TypeError (network down, giữ cookie + NextResponse.next()) vs 4xx (refresh invalid, xoá cookie + redirect login). Tránh logout user vì BE sập vài giây. (12) **M3 subscription-guard** — null guard `sub.expireAt?.slice() ?? 'kỳ trước'`. (13) **M7 subscription 400 catch** — bỏ catch 400 ở `getCurrentForOwner`, chỉ catch 404 (user thật sự không có sub). (14) **L1 audit-log validate** — `mapAction` check membership trong `ALLOWED_ACTIONS` array thay vì cast bừa. (15) **L2 forgotPassword console.error** — guard `NODE_ENV !== 'production'`. (16) **L3 leads page** — 3-state distinguish: `live` (data thật) / `empty` (BE OK nhưng 0 lead → show empty state, không DEMO) / `demo-error` (BE fail → show DEMO + error message rõ). Banner đổi sang rose color khi error. Typecheck **0 errors**. **Pending cần BE**: nothing critical — booking entity rename sang spec naming (customerName) là design choice, hiện adapter đủ tốt; subscription port rename `subscriptionId`→`ownerId` cosmetic, không gấp. |
| 2026-07-01 | be-v1.19-session-kick-handling | **Xử lý phiên bị đá — `/auth/refresh` trả 403 (§1.6.1.4).** Khi user login ở web/mobile khác cùng loại → refresh token bị thu hồi → `/auth/refresh` trả **403** `invalidRefreshToken`. FE phải clear token + báo "Tài khoản đã đăng nhập ở thiết bị khác. Vui lòng đăng nhập lại." + về `/login` (KHÔNG retry). Xử lý nhất quán ở **cả 3 đường** phiên có thể chết: (1) **api-client** [api-error.ts](src/infrastructure/http/api-error.ts) thêm class `SessionEndedError` + `code?` vào `ApiErrorPayload`; [api-client.ts](src/infrastructure/http/api-client.ts) `refreshTokens()` gặp 403 → `clearTokens()` + throw `SessionEndedError`; block retry-401 **propagate** `SessionEndedError` (không nuốt thành 401 chung). [auth.ts](src/app/actions/auth.ts) `getCurrentProfile()` bắt `SessionEndedError` → `redirect('/login?reason=session-ended')` (đường render layout/page + Server Action). (2) **middleware** [middleware.ts](src/middleware.ts): `refreshAtBackend` phân biệt 403 → `'session-ended'` (vs 401 → null vs 5xx → network-error), redirect kèm `?reason=session-ended` + xoá cookie (đường điều hướng sau khi access cookie hết hạn). (3) **chat socket** [chat-socket-provider.tsx](src/components/chat/chat-socket-provider.tsx): `refreshChatTokenAction()` trả `{ok:false, kicked:true}` khi refresh 403 → provider `disconnectChatSocket()` + `router.replace('/login?reason=session-ended')` (đường socket `tokenExpired` khi user đứng yên 1 trang, không điều hướng). (4) [login/page.tsx](src/app/(auth)/login/page.tsx) đọc `?reason=session-ended` → banner amber thông báo. (5) **Fix TTL**: refreshToken cookie maxAge 7→**14 ngày** ([api-config.ts](src/infrastructure/http/api-config.ts) + middleware) khớp BE §1.7 (tránh browser xoá cookie sớm gây logout oan). Typecheck **0 errors**, lint 0 warning mới (2 warning `api-client` unused-eslint-disable là pre-existing, chỉ dịch số dòng). |
| 2026-07-01 | be-v1.19-clienttype + chat-error-shape | **Rà soát API_SPEC_FULL v1.19 (BE cập nhật 2026-07-01) — session tách theo `clientType`.** (1) **§1.6.1 — Session split**: BE giới hạn 1 phiên web + 1 phiên mobile song song (không cho 2 web hoặc 2 app). FE web nên gắn header **`X-Client-Type: web`** khi login/refresh. → Thêm `'X-Client-Type': 'web'` vào default headers [api-client.ts](src/infrastructure/http/api-client.ts) (gắn mọi request — harmless, BE bỏ qua ở endpoint không cần). Rủi ro thấp: `/auth/login|register|google` vốn default 'web' khi thiếu header; web không dùng `/auth/apple` (endpoint duy nhất default 'mobile'). (2) **§17.4 chat `error` shape**: spec ghi `{ code?, message }` — `code` OPTIONAL (không phải `message`). Sửa `ChatSocketErrorPayload` [chat-socket.ts](src/lib/chat-socket.ts) `code: string` → `code?: string` + fix call `chatErrorMessage(p.code ?? '')` trong [chat-thread.tsx](src/components/chat/chat-thread.tsx). Xác nhận phần chat vừa làm KHỚP spec §17 (connect-after-login, tokenExpired refresh+reconnect, `read`/typing/presence events, không có key riêng cho socket — §17.7). (3) **CHƯA làm — cần quyết định UX**: §1.6.1.4 yêu cầu khi `/auth/refresh` trả **403** (phiên bị đá do login thiết bị khác, `code:'invalidRefreshToken'`) → clear tokens + toast "Tài khoản đã đăng nhập ở thiết bị khác. Vui lòng đăng nhập lại." + redirect login. Hiện web: refresh fail → `getCurrentProfile` trả null → layout redirect `/login` (đã về login nhưng **thiếu toast message riêng**). refreshToken TTL cũng đổi 7→**14 ngày** (chỉ cần sửa doc). Typecheck **0 errors**. |
| 2026-07-01 | chat-socket-contract-align | **Đồng bộ chat WebSocket theo contract chung App+Web (§17.4) — fix 2 gap web hay sai.** Rà soát code cũ: socket chỉ connect **lazy** khi mở trang hội thoại (`<ChatThread>`) + payload `error` sai shape (`{message}` thay vì `{code}`) + **thiếu flow `tokenExpired`** (accessToken TTL 15' hết hạn → web đứng hình, không tự refresh). **Xác nhận: KHÔNG có key/secret riêng cho socket — `auth.token` dùng CHÍNH accessToken REST** (`/auth/login` + `/auth/refresh` trả). **Fix**: (1) **Mới** [chat-socket-provider.tsx](src/components/chat/chat-socket-provider.tsx) — `ChatSocketProvider` + context `useChatConnection()`: connect NGAY khi vào khu vực quản lý (sau login), giữ 1 connection sống suốt phiên (nhận `message:new`/`presence` ở mọi trang, không chỉ trang chat). Xử lý `error{code:'tokenExpired'}` → gọi `refreshChatTokenAction()` (REST) → set token mới → getChatSocket recreate + reconnect (guard refresh storm bằng ref). Tear-down khi rời route group (logout). (2) Mount Provider vào [(host)/layout.tsx](src/app/(host)/layout.tsx) + [(admin)/layout.tsx](src/app/(admin)/layout.tsx) (đọc `readTokens()` song song profile). (3) [chat-socket.ts](src/lib/chat-socket.ts): `ChatSocketErrorPayload` đổi `{message}` → `{code, message?}` + helper `chatErrorMessage(code)` (tiếng Việt). (4) [use-chat-socket.ts](src/lib/use-chat-socket.ts): `useChatSocket` (tự quản connection) → **`useChatEvents(socket, handlers)`** (chỉ subscribe event trên socket cho sẵn). (5) **Mới** [auth.ts](src/app/actions/auth.ts) `refreshChatTokenAction()` — đọc refreshToken cookie → `authRepository().refresh()` → ghi cookie mới → trả accessToken cho client (client không đọc được httpOnly cookie). (6) [chat-thread.tsx](src/components/chat/chat-thread.tsx): bỏ prop `accessToken` + `wsOpts`, dùng socket từ context; `onError` đọc `p.code` (bỏ qua tokenExpired vì Provider tự reconnect); **emit socket `read`** khi connected (peer nhận `read:update` realtime — giống app), giữ REST `markConversationReadAction` cho badge lúc reload. (7) [messages/[id]/page.tsx](src/app/(host)/host/messages/[id]/page.tsx) bỏ truyền `accessToken`. **CHỜ App/BE confirm (không chặn)**: (a) `error` còn code nào ngoài `tokenExpired`? (b) REST `PATCH /conversations/:id/read` có broadcast `read:update` không (nếu có thì socket `read` emit là dư nhưng vô hại). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-06-05 | spec-v1.2-phase-8-partial | **Wire UI pages cho module mới — Leads list page**. `/host/leads/page.tsx` thay `getLeads()` static DEMO bằng `listLeadsAction()` từ Clean Arch path. Thêm adapter `toCardData(lead: Lead)` map entity field (camelCase) → `LeadCardData` (snake_case legacy). Fallback DEMO khi: (a) action fail (BE chưa sẵn / unauthorized / 500), (b) BE trả mảng rỗng (đang phát triển). Thêm banner amber `Lightbulb` báo "Dữ liệu demo" khi đang fallback để dev nhận biết. Typecheck **0 errors**. **Pages chưa wire** (defer phase sau, tránh risk): `/host/leads/[id]/page.tsx` (entity divergence — UI expects `responded_at/property_slug/base_price/weekend_price` BE không trả), `/admin/permissions/page.tsx` (component hardcoded 5 group × N permission keys ≠ spec 4-module × 4-CRUD-bit — cần rewrite component), `/host/messages/*` (cần Conversation entity adapter + dùng `useChatSocket` hook đã viết Phase 6c). |
| 2026-06-05 | spec-v1.2-phase-7 | **Chat REST Server Actions + Property shape verify**. (1) `app/actions/conversations.ts` — 9 Server Action backed by `chatRepository()` port: `listConversationsAction`, `getUnreadCountAction`, `createConversationAction`, `getConversationAction`, `listMessagesAction` (cursor), `sendMessageAction` (REST fallback, BE tự broadcast WS), `markConversationReadAction`, `editMessageAction`, `deleteMessageAction`. Zod validate đầy đủ: attachment phải `https://` + ≤5 file + ≤2048 chars URL, content ≤5000 chars, conversationId/messageId UUID. Legacy `actions/chat.ts` giữ nguyên cho `message-composer.tsx` chưa migrate. (2) **Verify BE shape**: curl `GET /properties/public` 200 OK — response 100% khớp `Property` entity hiện tại (type numeric, moderationStatus string, đầy đủ field §4.5). Tin tưởng spec cho property. (3) **Booking/Subscription shape vẫn pending verify** — cần access token để test thực tế `/bookings`, `/admin/subscriptions`, `/admin/users/:id/subscription`. Booking entity dùng `guestName/checkInAt/totalPrice/deposit` vs spec `customerName/checkinDate/totalAmount/depositAmount` — nguy cơ runtime mismatch khi gọi list/getById/hold (BE có thể trả nguyên spec shape → entity field sẽ undefined). **Khuyến nghị tiếp theo**: tạo credential test, curl 1 booking, quyết định rename entity hoặc thêm adapter trong ApiBookingRepository. (4) **Phase 7c — Entity refactor skip lần này** (AdminUser/KycAdmin/Dispute): các entity có aggregation fields `bookingCount/propertyCount/disputeCount/lastActiveAt` BE không cấp + lifecycle state đa dạng vs spec đơn giản. Cần roadmap riêng để: hoặc (a) bỏ aggregation field khỏi UI (tác động `welcome-checklist`, `/admin/properties`, `/admin/page`), hoặc (b) BE bổ sung endpoint stats. Typecheck **0 errors**. |
| 2026-06-05 | spec-v1.2-phase-6 | **Booking endpoints còn thiếu + Permissions module + Chat WebSocket client**. (1) **Booking** (spec §5.1 missing): port + Api repo + Mock thêm 5 method `listMine` (GET /my-bookings), `monthCalendar` (GET /calendar/:propertyId?year&month), `customerHold` (POST /customer-hold cho CUSTOMER 24h), `customerCancel` (PATCH /:id/customer-cancel), `update` (PUT /:id với body customerName/Phone/guestCount/notes/depositAmount). Mock delegate `listMine→list`, `customerHold→hold`, `customerCancel→cancel`. (2) **Permissions** (full stack mới — spec §12): `core/entities/permission.ts` (4 module `properties\|bookings\|calendar\|reviews` × 4 CRUD bit), `application/ports/permission-repository.ts` (getForUser + update), `ApiPermissionRepository` (GET/PUT /permissions/:userId với normalize empty rows cho 4 module), container default `api`, `app/actions/permissions.ts` (getPermissionsAction + updatePermissionsAction với Zod). (3) **Chat WebSocket** (spec §17.4): cài `socket.io-client@4.8.3`. `lib/chat-socket.ts` — singleton per tab, auto-reconnect (1s→8s backoff), connect tới `/chat` namespace với `auth: { token }` + `query: { lang }` từ accessToken. Type-safe event subscription helpers cho 7 server→client event: `message:new`, `message:ack`, `message:edit`, `message:delete`, `read:update`, `typing`, `presence`, `error`. Token rotate → disconnect+recreate. Emit helpers: `sendChatMessage`, `emitChatRead`, `emitTypingStart/Stop`. `lib/use-chat-socket.ts` — React hook wrapper với ref-stable handlers (không re-subscribe mỗi render) + connected state. Typecheck **0 errors**. **Còn lại Phase 7**: entity refactor AdminUser/KycAdmin/Dispute (cần UI work), verify Booking field names + Subscription userId mapping với BE thật, KYC upload nếu cần trên web. |
| 2026-06-05 | spec-v1.2-phase-5 | **Modules mới**: Leads + Admin Emails real + Chat REST. (1) **Admin Emails**: `sendTestEmailAction` đảo default sang `live` (spec §16 endpoint live), gọi `POST /admin/emails/test` qua apiClient khi không bị tắt bằng `NEXT_PUBLIC_EMAIL_LIVE=false`. (2) **Leads** (full stack mới theo spec §15): `core/entities/lead.ts` (5-state + 4-source), `application/ports/lead-repository.ts` (create Public + list/getById/update Auth), `ApiLeadRepository` (POST /leads skipAuth, GET/PATCH /leads), `MockLeadRepository` in-memory, container default `api`. `app/actions/leads.ts` Server Actions với Zod validate (5 method) — note: file legacy `actions/lead.ts` giữ nguyên cho route public `/property/:id/book` chưa migrate. (3) **Chat REST** (spec §17.1, WS client để session sau): `core/entities/chat.ts` (Conversation/ConversationMember/Message/MessageAttachment + type/role enum), `application/ports/chat-repository.ts` (9 method: list/unread-count/create/get/listMessages cursor/sendMessage/markRead/editMessage/deleteMessage), `ApiChatRepository` đầy đủ 9 endpoint REST, container hardcode `api`. **WS chưa wire** — cần Socket.IO client + connection manager + event handlers (`message:new`, `message:ack`, `message:edit`, `message:delete`, `read:update`, `typing`, `presence`) — defer Phase 6. Typecheck **0 errors**. |
| 2026-06-05 | spec-v1.2-phase-4 | **Subscription rewrite 4-state → spec §10.5 7-state** (BREAKING entity, backward-compatible field expand). (1) `SubscriptionStatus` đổi `paid\|pending\|overdue\|frozen` → `none\|trial\|active\|past_due\|cancelled\|frozen\|expired`. (2) `Subscription` entity thêm optional fields theo spec §10: `nextChargeAt`, `trialEndsAt`, `priceOverride`, `provider` (`apple_iap\|vnpay\|manual_bank\|manual\|null`), `frozenAt`, `frozenReason`. Giữ `expireAt` làm alias của `nextChargeAt` để UI không vỡ. (3) `subscription-guard.ts` `blockedReason()` rewrite — chặn 4 trạng thái `frozen\|past_due\|expired\|cancelled` với message phù hợp; cho phép `none\|trial\|active`. (4) Mock seed: status literal `paid→active`, `pending→none`, `overdue→past_due`. Method `markPaid`/`unfreeze` map sang state mới. (5) UI updates: `/admin/payments` STATUS_LABEL+VARIANT+TABS+parseStatus cover 7 state mới, filter `pending→past_due`; `/host/settings/subscription` STATUS_LABEL+VARIANT, hint trỏ `past_due`; `/host/billing` literal `overdue→past_due`; `subscription-row-actions.tsx` action visibility map sang state mới (`active\|trial\|past_due` cho mark-paid + freeze, `past_due` cho call). (6) `ApiSubscriptionRepository` spec §10.4 — list/getById (via `/admin/users/:id/subscription`)/getCurrentForOwner (via `/subscriptions/me`)/count-overdue/sum-paid/mark-paid/freeze/unfreeze. Map BE response (`planId`, `endsAt`, `nextChargeAt`, `trialEndsAt`, etc.) sang local shape. **Lưu ý** BE identify theo `userId` chứ không phải `subscriptionId` — port hiện giả định `subscriptionId === ownerId` (1-1). (7) Container default `subscriptionRepository()` vẫn = `mock` (do mapping userId chưa verify end-to-end), bật `api` qua env `NEXT_PUBLIC_DATA_MODE_SUBSCRIPTIONS=api`. Typecheck **0 errors**. |
| 2026-06-05 | spec-v1.2-phase-3-partial | **Wire mock→API cho Calendar + Reviews** (clean shape mapping). (1) `ApiCalendarRepository` spec §6: `getGrid` map `startDate/endDate/propertyId` query + spec string status `available\|hold\|booked\|locked` → local numeric `CalendarStatus` enum; `lockDate`, `unlockDate` (DELETE body), `markSold`. `list()` legacy event API trả mảng rỗng. (2) `ApiReviewRepository` spec §7.2: `list` map status filter `published→visible/hidden→hidden/undefined→all`, response §7.3 → local Review shape (`avgRating` → `rating` round, hydrated fields fallback empty), `countFlagged`, `hide` (DELETE body reason), `restore`. (3) Container: `calendarRepository()` + `reviewRepository()` default sang `api`, mock fallback qua env. **Skip 4 module còn lại** (lý do entity divergence cần UI refactor riêng): `ApiAdminUserRepository` (entity có aggregation `bookingCount/propertyCount/disputeCount` spec không trả); `ApiKycAdminRepository` (entity có lifecycle 8-state `draft/payment_pending/paid/...` spec chỉ 4-state); `ApiDisputeRepository` (entity rich Vietnamese model với evidence/chatExcerpt/parties/verdict/penalty không khớp spec đơn giản); `ApiPermissionRepository` (chưa có port + entity). KYC host-side (`getStatus`) đã wire từ trước — spec §9.1 upload/submit/resubmit chỉ dùng trên mobile theo comment trong entity. Typecheck **0 errors**. |
| 2026-06-05 | spec-v1.2-phase-2 | **Audit log cleanup theo spec §14** (BE tự ghi, FE chỉ đọc). (1) Tạo `ApiAuditLogRepository` map spec response §14.3 → local shape: `targetType/targetId/targetLabel/createdAt/metadata.reason` → `target.{type,id,label}/at/reason`, action dot-slug `user.ban` → underscore `user_ban`. (2) Mở rộng `AuditAction` enum thêm 9 spec action: `user_delete`, `user_kyc_bypass_toggle`, `review_restore`, `booking_mark_paid`, `subscription_{trial_grant,trial_revoke,set_price,mark_paid,freeze,unfreeze}`. Đổi `user_revoke_session` → `user_revoke_sessions` (spec slug), `dispute_start_investigation` → `dispute_investigate`. (3) Mở rộng `AuditTargetType` thêm `booking`, `subscription`. (4) Bỏ method `record()` khỏi port + `recordAuditEntryUseCase` khỏi use case + `record()` khỏi MockAuditLogRepository. (5) Xoá `src/lib/audit-recorder.ts`. (6) Xoá `recordAudit()` calls trong 6 action file: `admin-users.ts` (6 calls), `admin.ts` (3), `disputes.ts` (3), `kyc-admin.ts` (2), `reviews.ts` (1), `subscriptions.ts` (3) — tổng 18 call sites + 6 import line. (7) Container default: `auditLogRepository()` → `api`, mock giữ làm fallback qua `NEXT_PUBLIC_DATA_MODE_AUDIT_LOG=mock`. (8) Cập nhật `/admin/audit-log` page: ACTION_VARIANT cover 24 action, TARGET_ICON + TARGET_COLOR thêm booking (CalendarCheck/sky) + subscription (CreditCard/violet), `parseTarget()` mở rộng. Typecheck **0 errors**. |
| 2026-06-05 | spec-v1.2-phase-1 | Rà soát `docs/API_SPEC_FULL.md` v1.2 (1204 dòng) đối chiếu code. Verify BE bằng curl: tất cả endpoint spec đều live (`/properties/public` 200, `/calendar/public-grid` 400 cần params, `/billing/plans` 200, `/app/version` 200, các endpoint admin 401 cần auth). `/dashboard/stats` + `/reports` vẫn live (spec sót, giữ). **Phase 1 — Quick wins (port + repo layer, không đụng UI)**: (1) `core/entities/user.ts` bổ sung field spec §2.3: `avatar`, `emailVerified`, `updatedAt`, `subscriptionProvider`, `subscriptionPriceOverride`, `subscriptionFrozenAt/Reason`. `SubscriptionStatus` enum đổi 5→7 state đúng spec §10.5 (`none\|trial\|active\|past_due\|cancelled\|frozen\|expired`) — thay literal `trialing` cũ. Thêm `SubscriptionProvider` type. (2) Bug fix: `ApiBookingRepository.markPaid` URL `/mark-paid` → `/paid` (spec §5.1). (3) `PropertyRepository` (port + Api + Mock) thêm: `listPublic(filters)` (Public §4.1), `getShare(id)` (Public §4.1), `approve/reject/suspend` (Admin §4.4). (4) `StaffRepository` (port + Api + Mock) thêm: `verifyInvite(token)` + `acceptInvite(input)` (Public §11.2 cho landing accept invite). Typecheck **0 errors**. **Phase 2–5 còn pending** (chờ phê duyệt từng phase do scope lớn): Audit log cleanup (spec §14 cấm FE ghi audit — phải xoá `recordAudit()` ở 12 file action + tạo `ApiAuditLogRepository` read-only), Wire mock→API (Calendar/Reviews/Disputes/Permissions/KycAdmin/AdminUser + expand Kyc upload+submit+resubmit), **Subscription entity rewrite BREAKING** (mock entity 4-state `paid\|pending\|overdue\|frozen` → spec 7-state, ảnh hưởng `/admin/payments` page + `/host/settings/subscription` + `/host/billing` + `subscription-guard.ts` + `subscription-row-actions.tsx`), New modules (Leads stack mới + Admin Emails real `POST /admin/emails/test` + Chat REST `/conversations/*` + Socket.IO `/chat` namespace). **Field mismatch flag**: `core/entities/booking.ts` dùng `guestName/checkInAt/totalPrice/deposit/nights` nhưng spec §5.3 dùng `customerName/checkinDate/totalAmount/depositAmount` — cần verify response thật BE trả về để quyết định adapter hay rename entity (risk cao, ưu tiên xác minh runtime trước khi sửa). |
| 2026-05-25 | supabase-final-cleanup | Loại bỏ hoàn toàn mọi reference Supabase còn sót: xoá thư mục `supabase/` (migrations + README), xoá env vars Supabase trong `.env.local.example`, xoá `*.supabase.co` khỏi `next.config.ts` (images + CSP), dọn `.gitignore`/`.prettierignore`/`eslint.config.mjs`. Rename `database.types.ts` → `legacy-types.ts` + cập nhật 11 file import. Cập nhật `README.md` và `docs/CODING-STANDARDS.md` thay mọi reference Supabase bằng Clean Architecture / REST API. Xoá prop `isSupabaseReady` khỏi `image-uploader.tsx`. Typecheck 0 errors, grep supabase trong src/ = 0 results. |
| 2026-06-11 | be-v1.12-apple-iap-trial | **Đồng bộ BE v1.12 — Apple IAP compliance + silent OWNER trial 60 ngày** (spec §2A.5, [RELEASE_NOTES_v1.12_APPLE_IAP.md](docs/RELEASE_NOTES_v1.12_APPLE_IAP.md)). Web giữ luồng thanh toán (§4.4), không như iOS. BE: OWNER đăng ký → `subscriptionStatus="trial"` + `trialEndsAt=now+60d`; gate entitlement ở `POST /properties`, `PUT /properties/:id`, `POST /staff/invites` → hết trial trả **403 generic `subscription.featureLocked`** ("Tài khoản chưa có quyền dùng tính năng này"). KYC vẫn bắt buộc để đăng phòng (lỗi riêng `kyc.propertyRequiresKyc`). **FE changes**: (1) **mới** [lib/entitlement.ts](src/lib/entitlement.ts) — `isFeatureLockedError(msg)` (nhận diện qua nội dung message vi/en vì BE không có error code riêng) + `ownerEntitlement(profile)` suy ra entitlement từ `GET /auth/profile` (nguồn tin cậy, không phụ thuộc subscription repo đang default mock): trả `{entitled, isTrial, trialDaysLeft, blockReason}`; conservative — `none`/`null` coi như entitled để tránh banner báo nhầm legacy free-tier. (2) [layout.tsx (host)](src/app/(host)/layout.tsx) — thay banner subscription dựa trên mock repo bằng banner profile-based: block banner (rose) khi hết trial/past_due/cancelled/expired/frozen + **countdown banner (sky) "Đang dùng thử — còn X ngày"** cho OWNER (web được phép hiển thị). Bỏ `getMySubscriptionAction` khỏi layout (giảm 1 call mock/page). (3) [properties/new/page.tsx](src/app/(host)/host/properties/new/page.tsx) — pre-block dùng `ownerEntitlement(profile)` thay `blockedReason(mockSub)`. (4) [property-wizard.tsx](src/components/host/property-wizard.tsx) + [invite-staff-form.tsx](src/components/host/invite-staff-form.tsx) — bắt 403 featureLocked → hiển thị message + CTA "Đăng ký / gia hạn gói" link tới `/host/settings/subscription` (invite form thêm inline notice vì toast không chứa link được). (5) [subscription-guard.ts](src/lib/subscription-guard.ts) — `blockedReason()` thêm nhánh `trial` hết hạn (`trialEndsAt <= now` → chặn) + refresh comment stale. Schema profile không đổi (§5 release notes). Typecheck **0 errors**, lint **0 warnings**. |
| 2026-06-13 | back-nav + bỏ duyệt cơ sở | **(1) Nút "Quay lại" cho trang chi tiết/con.** Thêm 2 prop `backHref` + `backLabel` vào [page-header.tsx](src/components/host/page-header.tsx) (render nút ← icon `ArrowLeft`, hover trượt). Gắn vào 9 trang chỉ có breadcrumb mà thiếu nút back: `admin/properties/[id]`, `admin/disputes/[id]`, `admin/payments/sessions`, `admin/settings/emails`, `host/bookings/[id]`, `host/bookings/new`, `host/properties/[id]`, `host/properties/[id]/images`, `host/properties/new`, `host/settings/subscription`. 4 trang đã có back từ trước (kyc/[id], users/[id], leads/[id], messages/[id]) giữ nguyên. **(2) 🔴 BREAKING business — Bỏ luồng duyệt cơ sở.** Quyết định: OWNER đã KYC tạo phòng là **active ngay**, không chờ admin duyệt (BE đang xử lý: `POST /properties` set `isActive=true` cho mọi role). FE gỡ toàn bộ khái niệm "Chờ duyệt": [admin/properties/page.tsx](src/app/(admin)/admin/properties/page.tsx) — `Status` chỉ còn `active|suspended|''`, bỏ `isPending` (suspended = `!isActive`, không còn phân biệt bằng `bookingCount`), bỏ stat tile + tab + header CTA "Chờ duyệt" (4→3 tile), badge `✓ Đang hoạt động` / `🔒 Tạm khoá`, bỏ accent `gold` không dùng. [approval-actions.tsx](src/components/admin/approval-actions.tsx) — rewrite chỉ còn suspend/unlock (`status: 'active'|'suspended'`), gỡ nhánh approve/reject + form lý do. [admin.ts](src/app/actions/admin.ts) — xoá `approvePropertyAction` + `rejectPropertyAction` (+ import `z`, `getPropertyByIdUseCase`), dọn dead code (`before`/`profile`/empty-if) trong `suspendPropertyAction`. [property-filters.tsx](src/components/admin/property-filters.tsx) — highlight count đổi `pending` (gold) → `suspended` (rose). Repo method `.approve()/.reject()` + endpoint BE `/approve`,`/reject` giữ nguyên (FE không gọi). Ghi quyết định vào [SPEC-FE-FOR-BE.md](docs/SPEC-FE-FOR-BE.md) §6.8. Typecheck **0 errors**. |
| 2026-06-13 | admin-bookings-ux + hold-null | **(1) Fix crash `/admin/bookings`**: mapper `ApiBookingRepository` ép `vnd(totalAmount/depositAmount)` → BE trả `null` cho đơn HOLD (chưa chốt giá) ném `vnd: invalid number null` làm sập cả trang. **(2) 🔴 Entity BREAKING**: `Booking.totalPrice` + `Booking.deposit` đổi `VND` → `VND \| null` (HOLD chưa join bảng giá; chỉ chốt khi `markPaid`). Mapper giữ `null` thay vì coerce 0 (theo yêu cầu BE: không hardcode `?? 0`). **(3)** Helper mới trong [lib/booking-display.ts](src/lib/booking-display.ts): `BOOKING_STATUS_LABEL/VARIANT` (dùng chung), `formatBookingTotal()` (`null` → "Chưa chốt giá"), `holdSecondsLeft()` (ưu tiên `holdRemainingSeconds` server-side, fallback `holdExpireAt`). Cập nhật consumer null-safe: host/admin booking detail (remaining/fullyPaid guard null), host+admin list, 2 dashboard, mock repo arithmetic. **(4)** Redesign `/admin/bookings`: 4 stat tile + tab lọc status (URL `?status=`) + bảng mới. Component client [booking-row.tsx](src/components/admin/booking-row.tsx) cho phép click cả dòng → chi tiết (Enter/Space accessible, SĐT bấm gọi riêng). **(5)** Trang chi tiết admin MỚI [admin/bookings/[id]/page.tsx](src/app/(admin)/admin/bookings/[id]/page.tsx) (trước đây 404). **(6)** Countdown HOLD (host + admin) chuyển sang `holdRemainingSeconds`. **BE đã confirm** (API_SPEC_FULL §5): `GET /bookings/:id` live (detail có thêm `property.images[]` + `property.owner`); 4 mutation hold/confirm/paid/cancel đều live; `PATCH /cancel` hiện BỎ QUA `reason`. **Còn chờ FE quyết** (báo BE): (a) BE tự tính `totalAmount` lúc HOLD? (b) BE lưu `reason` khi cancel? Typecheck **0 errors**, lint 0 warning mới. |
| 2026-06-13 | payments-real-api + reconcile-simplify | **(1) 🔴 `/admin/payments` chuyển MOCK → API thật.** Container default `subscriptionRepository()` đổi `mock` → `api` (override `NEXT_PUBLIC_DATA_MODE_SUBSCRIPTIONS=mock`). **(2) Fix mapper sai shape**: `ApiSubscriptionRepository` trước đây map field không tồn tại (`ownerId/ownerName/amount/roomCount`). `GET /admin/subscriptions` thực tế trả **user-level** (spec §A4): `id`=userId, `name`/`email`, `subscriptionStatus`, `subscriptionPlanId` (`rooms_5`), `subscriptionCycle`, `subscriptionPriceOverride`, `nextChargeAt`, frozen fields. Mapper viết lại: `roomsFromPlanId('rooms_5')→5`, `plan` suy từ `planForRoomCount`, `amount = priceOverride ?? calcSubscriptionAmount(plan,rooms,cycle)` (tôn trọng `priceOverride=0`=miễn phí), `ownerId = id`, đọc cả họ field user-level lẫn legacy/snapshot để không vỡ ở `/admin/users/:id/subscription`, `mark-paid`, `freeze`, `/subscriptions/me`. **(3) Entity** `Subscription` thêm `ownerEmail?: string\|null` (BE list có email). **(4) Trang `/admin/payments` rõ ràng hơn**: bỏ cột "Kỳ" (vô nghĩa với user-level), hiện email cạnh tên thay UUID, badge gói + "Theo tháng/năm", cột "Phí / kỳ" hiện "Miễn phí" khi 0, cột "Charge tiếp theo" guard rỗng, tiêu đề "Gói cước chủ nhà" + mô tả rõ luồng doanh thu, nút "💸 Đối soát chuyển khoản". **(5) `/admin/payments/sessions` đơn giản hoá mark-paid**: bỏ ô nhập "Mã giao dịch banking" trong `MarkSessionPaidButton` — giờ chỉ confirm (hiện số tiền + nội dung CK) → `POST /admin/payments/:id/mark-paid` không body → kích hoạt gói. Cột hành động cho session đã thu hiện badge "✓ Đã nhận tiền" thay vì mã ref. Banner cập nhật "không cần nhập mã giao dịch". `reference` vẫn giữ optional ở entity/action cho BE compat. Typecheck **0 errors**, lint **0 warnings**. **Lưu ý**: đổi default subscription sang api cũng ảnh hưởng `/host/settings/subscription` (cùng repo) — mapper tolerant nên không vỡ. |
| 2026-06-13 | payments-be-align | **Đồng bộ BE phản hồi (deploy 2026-06-13).** (1) BE bổ sung `rooms` + `amount` (VND đã gồm VAT, cùng công thức `POST /payments/quote`, `null` khi chưa có plan) vào `GET /admin/subscriptions` → FE đọc thẳng, bỏ tính tiền client-side. Mapper `ApiSubscriptionRepository` đã sẵn fallback `s.rooms`/`s.amount`; nới kiểu `amount?: number \| null` (BE trả null → `??` rơi về công thức). (2) **🐛 Fix shape `sum-paid`**: BE trả `{ totalPaid, count, from, to }` (không phải `{ total }`) — repo trước đọc `data.total` → `undefined`, StatCard "Đã thu tháng này" hiển thị sai. Sửa đọc `data.totalPaid`. `count-overdue` trả `{ count }` (đã đúng), hardened `?? 0`. (3) Confirm các điểm khác không cần đổi: `mark-paid` subscription chỉ cần `{ amount }` (BE tự suy plan/cycle/rooms/days, không validate khớp công thức → không lo 400 lệch giá); `mark-paid` session nhận body rỗng OK; `/subscriptions/me` trả tên field đầy đủ (`subscriptionPlanId/Status`) — mapper đã đọc đúng nhánh đầy đủ; SALE chưa gán → BE 400 `saleNotAssigned`, `/host/settings/subscription` render empty state an toàn (action lỗi → `sub=null`). Typecheck **0 errors**, lint **0 warnings**. |
| 2026-06-13 | sessions-ui-declutter | **Dọn UI trang `/admin/payments/sessions` (đối soát chuyển khoản).** (1) **Cột "Gói cước"**: bỏ badge tím hiện mã kỹ thuật `rooms_5`, thay bằng tên dễ đọc — entity `PaymentSession` thêm `planLabel` (BE trả `planLabel` "Gói 5 phòng" theo spec §10.3; fallback helper `paymentSessionPlanLabel(planId, rooms)` suy "Gói N phòng" / "Doanh nghiệp"). Mapper điền `r.planLabel ?? fallback`. Hiển thị: tên gói + dòng phụ "Theo tháng/năm" (bỏ "5 phòng" trùng lặp). (2) **Cột "Trạng thái"**: bỏ ngày `paidAt` + note "Quá hạn 24h" dưới badge (đỡ rối, ngày hết hạn đã có cột riêng). (3) **Cột "Nội dung CK"**: component mới [copyable-code.tsx](src/components/admin/copyable-code.tsx) (client) — chip 1 dòng cắt gọn (`truncate` + tooltip full), **click để copy** nguyên văn vào clipboard (Check icon feedback 1.5s). Trước đó icon Copy chỉ trang trí + chuỗi dài wrap 2 dòng làm row cao. Typecheck **0 errors**, lint **0 warnings**. |
| 2026-06-13 | sessions-pagination | **Phân trang `/admin/payments/sessions`.** Thêm `<Pagination>` (reuse component, PAGE_SIZE=10) client-side trên tập `visibleSessions` đã lọc — giống pattern `/admin/payments`. URL `?page=`, `pageHref` giữ `status`+`q`; đổi tab/search tự reset về trang 1 (href không kèm page). BE fetch tối đa `limit=100`. Typecheck **0 errors**, lint **0 warnings**. |
| 2026-06-13 | reports-real-api | **`/admin/reports` chuyển MOCK → API thật (100%).** Trang trước đây fabricate 5 KPI §7 + 2 bảng demo (`getMock`). BE chưa có `/admin/reports/risk-kpis` và 3/5 chỉ số §7 (host-cancel, no-show, deletion-request) không có endpoint nào → thiết kế lại quanh dữ liệu thật đang có. **6 KPI card** (Promise.all): Tổng việc cần xử lý (sum), Dispute đang mở (`countActiveDisputesAction`), Review bị gắn cờ (`countFlaggedReviewsAction`), KYC chờ duyệt (`countPendingKycAdminAction`), Subscription quá hạn (`countOverdueSubscriptionsAction`), Doanh thu đã thu tháng này (`sumPaidSubscriptionsAction` + `monthRange`). Mỗi card là Link tới trang xử lý tương ứng + CTA. Bỏ Sparkbar/delta giả. **2 bảng thật**: "Dispute đang chờ" (`listDisputesAction` lọc open/investigating, sort desc, top 6, link `/admin/disputes/[id]`) thay bảng recent-disputes mock; "Review bị gắn cờ" (`listReviewsAction({flagged:true})` top 6) thay bảng top-host-cancel (không có endpoint booking-analytics). Bỏ range selector (KPI là snapshot hiện tại, chỉ doanh thu theo tháng). Banner amber khi action nào fail (không im lặng hiển thị 0). Typecheck **0 errors**, lint **0 warnings**. |
| 2026-06-14 | gộp 1 hệ giá gói cước (owner = danh mục admin) | **Gỡ bỏ hệ giá per-room thừa ở phía chủ nhà — chủ nhà giờ thấy đúng danh mục gói admin quản lý.** Trước đây tồn tại 2 hệ: (a) danh mục `BillingPlan` (`/billing/plans`, `rooms_*`, giá tháng/năm) admin sửa ở `/admin/pricing`; (b) hằng số hardcode `PRICE_PER_ROOM` (50k/phòng) — chủ nhà bị tính theo (b), khác hẳn (a). **Sửa**: (1) Entity [subscription.ts](src/core/entities/subscription.ts) thêm `Subscription.planId` (id gói thật, nguồn chuẩn để tra tên/giá; `plan` bucket free/basic/... giờ chỉ còn là phân loại nội bộ admin). (2) [api-subscription-repository.ts](src/infrastructure/repositories/api-subscription-repository.ts) map `planId = subscriptionPlanId ?? planId`. (3) Mock repo thêm `planId` cho 5 seed. (4) **Rewrite** [tier-selector.tsx](src/components/host/tier-selector.tsx): nhận `plans: BillingPlan[]` + `currentPlanId`, render thẳng danh mục (giá tháng/năm thật, "—" khi BE chưa cấu hình, tiết kiệm % khi trả năm), bỏ toggle/per-room math + bỏ modal "thành công" giả → dialog hiện thông tin chuyển khoản thật. (5) [host/billing](src/app/(host)/host/billing/page.tsx): fetch `listBillingPlansAction()`, hero hiện tên gói theo `planId` + **chi phí kỳ = `sub.amount` BE trả** (bỏ `roomCount × PRICE_PER_ROOM`), bỏ `ROOM_RANGE`. (6) [host/settings/subscription](src/app/(host)/host/settings/subscription/page.tsx): nhãn gói theo `planLabel(sub.planId)`, bỏ dòng per-room + bảng giá hardcode → link sang trang Gói cước. (7) [admin/payments](src/app/(admin)/admin/payments/page.tsx): cột gói hiện `planLabel(s.planId)` (đồng bộ tên với danh mục) thay nhãn bucket suy từ roomCount. **Còn lại (admin-internal, cần BE để bỏ hẳn)**: enum `SubscriptionPlan`/`PRICE_PER_ROOM`/`planForRoomCount`/`calcSubscriptionAmount` vẫn giữ cho mapper suy `plan` bucket (màu badge) + mock seed + tính năng "đổi gói" trong user-moderation (cần endpoint BE set planId mới gỡ được). **BE cần làm để chủ nhà thấy đủ giá**: điền `monthlyPrice` + `rooms` cho từng gói trong danh mục (hiện chỉ có giá năm → cột giá tháng/số phòng ra "—"). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-06-24 | bookings-new-empty-state-ux | **Sửa thông báo `/host/bookings/new` lộ jargon + gộp nhầm 2 tình huống.** Trước: khi `properties.length===0` hiện "Bạn chưa có cơ sở nào **hoặc API không kết nối được**" — vừa lộ jargon kỹ thuật (không hợp người dùng), vừa gộp 2 case khác hẳn (thật sự chưa có cơ sở vs tải lỗi). **Fix** [bookings/new/page.tsx](src/app/(host)/host/bookings/new/page.tsx): tách `loadFailed = !result.ok` → (a) lỗi tải = banner rose "Không tải được danh sách cơ sở. Vui lòng tải lại trang…" + nút "Tải lại"; (b) thật sự rỗng = empty-state "Bạn chưa có cơ sở nào để nhận đặt phòng. Hãy tạo cơ sở (homestay, khách sạn, căn hộ…) trước…" + CTA. Rà thêm 2 chỗ khác hiện "Không kết nối được API:" ([host/page.tsx](src/app/(host)/host/page.tsx), [host/properties/page.tsx](src/app/(host)/host/properties/page.tsx)) → đổi thành "Tạm thời không tải được dữ liệu:". Typecheck **0 errors**. |
| 2026-06-24 | kyc-status-card-crash-fix | **🐛 Fix crash `/host/settings` (+ mọi trang render `KycStatusCard`) cho OWNER admin cho pass KYC.** Lỗi thật (lấy từ stack qua dev error overlay): `TypeError: Cannot read properties of undefined (reading 'bg')` tại [kyc-status-card.tsx](src/components/host/kyc-status-card.tsx). Nguyên nhân: `STATUS_TONE[kycStatus]` chỉ map theo **vocab submission** (`draft|kyc_submitted|payment_pending|paid|awaiting_approval|approved|rejected|refunded|none`) nhưng `/kyc/status` trả `kycStatus` theo **vocab User profile** (`none|pending|approved|rejected` — xem `KycStatus` [user.ts](src/core/entities/user.ts)) → giá trị `'pending'` không có trong map → `tone=undefined` → `.bg` sập. **Fix**: (1) `STATUS_TONE` đổi key sang `string` + thêm entry `pending` (tone xanh "⏳"); fallback `?? STATUS_TONE.none` + `label ?? 'Đang cập nhật'` + `console.warn` dev khi gặp status lạ → không bao giờ sập nữa. (2) [kyc.ts](src/core/entities/kyc.ts) thêm type `KycStatusValue = KycSubmissionStatus | 'none' | 'pending'`, `KycStatusResponse.kycStatus` dùng type này, `KYC_STATUS_LABEL` thêm `pending: 'Đang chờ duyệt'`. (3) [host/error.tsx](src/app/(host)/error.tsx) thêm khung hiện `error.message`+stack khi dev (chỉ dev — công cụ chẩn đoán, giữ lại). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-06-24 | host-billing-crash-fix | **🐛 Fix crash `/host/billing` khi login OWNER (admin cho pass KYC).** Nguyên nhân: `formatDate`/`formatDateTime` ([lib/format.ts](src/lib/format.ts)) gọi `Intl.DateTimeFormat().format(d)` với **Invalid Date** → `Intl` ném `RangeError: Invalid time value` → sập trang (error boundary host). OWNER mới pass KYC: `/subscriptions/me` trả sub nhưng `nextChargeAt`/`trialEndsAt`/`createdAt` rỗng → mapper tính `expireAt=''` → dòng "Hết hạn: {formatDate(sub.expireAt)}" (hero) crash. **Fix**: `formatDate`+`formatDateTime` guard `null`/`''`/Invalid Date → trả `'—'` (sửa app-wide, mọi call-site an toàn). [host/billing/page.tsx](src/app/(host)/host/billing/page.tsx) chỉ render dòng "Hết hạn" khi `sub.expireAt` có giá trị (tránh "Hết hạn: —" trống). Typecheck **0 errors**. |
| 2026-06-24 | pricing-field-map-fix | **🐛 Fix `/admin/pricing` bảng sửa hiện "—" ở Giá tháng + Phòng, sửa giá tháng không lưu được.** Nguyên nhân thật (đính chính chẩn đoán cũ ở entry `pricing-ux`): endpoint admin `GET /admin/billing-plans` trả tên trường **legacy** (`minCharge`/`maxRooms`/`pricePerRoom`) còn public `GET /billing/plans` đã chuẩn hoá (`monthlyPrice`/`rooms`) — verify bằng curl. `ApiBillingPlanRepository` trước pass-through raw nên chỉ `yearlyPrice` (trùng tên) hiện đúng; `monthlyPrice`/`rooms` undefined → "—". Code ghi cũng gửi sai (`monthlyPrice`/`rooms`) trong khi BE chờ `minCharge`/`maxRooms` (spec §10.1) → Lưu giá tháng bị bỏ qua. **Fix** [api-billing-plan-repository.ts](src/infrastructure/repositories/api-billing-plan-repository.ts): `mapPlan()` đọc tolerant 2 tên (`monthlyPrice ?? minCharge`, `rooms ?? maxRooms`, `maxRooms===null → -1`) cho cả `list`+`listAll`; `toWriteDto()` ghi đúng tên spec (`minCharge`/`maxRooms`/`pricePerRoom` tự tính + `name` từ `planLabel`) cho create+update; `delete` đọc `softDeleted` (spec §10.1) thay field `mode` không tồn tại. Typecheck **0 errors**. |
| 2026-06-14 | pricing-ux + ẩn rò rỉ endpoint | **Trang `/admin/pricing` (gói cước — dùng API thật, không phải mock).** (1) **Gỡ text làm lộ endpoint/jargon kỹ thuật** khỏi UI: [billing-plan-editor.tsx](src/components/admin/billing-plan-editor.tsx) đổi tiêu đề "Catalog admin (read/write)" → **"Danh mục gói cước"**, bỏ subtitle "CRUD trực tiếp qua `/admin/billing-plans`. Bao gồm gói inactive (soft-deleted)" → mô tả nghiệp vụ; toast/ConfirmDialog/AdminWriteHint bỏ "ref/soft delete/BE" → ngôn ngữ người dùng ("đang có người dùng → ẩn gói"). [pricing/page.tsx](src/app/(admin)/admin/pricing/page.tsx): description + section "Public preview" (`/billing/plans`, "cache 5 phút") + Notes (`GET /billing/plans`, `User.subscriptionPlanId`) → diễn đạt nghiệp vụ, bỏ mã endpoint. (2) **Trường null/trống → "—"**: cột Phòng (rooms `== null` → "—", `-1` → "∞"), Giá tháng/năm (0 → "—"), Tính năng (rỗng → "—") ở cả bảng editor lẫn bảng preview + card. (3) **Rà soát trang tương tự — gỡ rò rỉ hiển thị**: [permissions/page.tsx](src/app/(admin)/admin/permissions/page.tsx) bỏ "Spec §12", "CRUD" → "thao tác (thêm/xem/sửa/xoá)", "/staff/invites" → "mời qua email"; [permission-editor.tsx](src/components/admin/permission-editor.tsx) bỏ "Spec §12"; [settings/emails/page.tsx](src/app/(admin)/admin/settings/emails/page.tsx) "(BE chưa có endpoint)" → "(sắp ra mắt)"; [test-email-button.tsx](src/components/admin/test-email-button.tsx) bỏ "BE chưa wire endpoint…log ra console" → mô tả nghiệp vụ; [host/bookings/[id]](src/app/(host)/host/bookings/[id]/page.tsx) bỏ "Khi BE bổ sung endpoint conversations". **Xác nhận `/admin/pricing` dùng API thật** (`ApiBillingPlanRepository` → `GET /billing/plans` + `/admin/billing-plans`), KHÔNG mock — cột Phòng trống do BE chưa trả `rooms` cho các gói đó (nay hiển thị "—"). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-06-14 | mock-cleanup + paginate-all | **(1) Bỏ dữ liệu demo cứng còn sót + chuẩn hoá empty/error state.** Helper mới [lib/pagination.ts](src/lib/pagination.ts) (`parsePage`/`pageCount`/`paginate`/`buildPageHref`) dùng chung cho Server Component. [host/guests](src/app/(host)/host/guests/page.tsx): xoá mảng `DEMO` 6 khách + `getGuests()` → empty-state "chờ BE" (module chưa có endpoint). [host/billing](src/app/(host)/host/billing/page.tsx): xoá mảng `invoices` bịa + component `InvoiceStatus`; lịch sử hoá đơn → empty-state (hero gói cước vẫn dùng dữ liệu thật). [host/leads](src/app/(host)/host/leads/page.tsx) + [host/leads/[id]](src/app/(host)/host/leads/[id]/page.tsx): **bỏ hẳn fallback DEMO** — lỗi BE giờ hiện banner lỗi rõ ràng / `notFound()`, không hiện dữ liệu giả. [host/messages](src/app/(host)/host/messages/page.tsx): bỏ fallback `DEMO_CONVERSATIONS`, **xoá file `demo-data.ts`**, đưa type `Conversation` vào page. (2) **Phân trang mọi bảng/list còn thiếu** (reuse `<Pagination>` + URL `?page=`): admin/bookings (PAGE_SIZE 20, giữ `?status=`), admin/properties (12, giữ status/q/type/sort), admin/disputes (10), host/bookings (15), host/properties (12), host/leads (10), host/messages (15), host/staff (10 cho cả 2 bảng — param riêng `?staffPage=`/`?invitePage=`). Các bảng đã có phân trang từ trước (admin users/payments/reviews/kyc/audit-log/payments-sessions) giữ nguyên. Typecheck **0 errors**, lint **0 warning mới** (chỉ còn warning pre-existing ở disputes/kyc-admin/subscriptions actions + emails). |
| 2026-06-14 | emails BE-driven (Q7 resolved) | **BE chốt Q7: BE là source of truth cho 15 key email, FE đổi theo, KHÔNG layer mapping.** 15 key chính thức: `welcome_owner, welcome_sale, password_reset, booking_confirmed, booking_cancelled, booking_paid, kyc_approved, kyc_rejected, staff_invite, subscription_due, subscription_overdue, subscription_paid, dispute_opened, review_received, property_approved` (định nghĩa BE `email.service.ts`). `POST /admin/emails/test` reject key lạ. → **(1) Xoá toàn bộ stack render email song song của FE** (drift-prone, sai/thiếu key): 6 file `src/lib/emails/*` (`booking-confirmation`, `payment-received`, `booking-transitions`, `simple-templates`, `samples`, `types`) + route `src/app/api/admin/emails/preview/[template]/route.tsx`. (2) **Trang [/admin/emails](src/app/(admin)/admin/emails/page.tsx) render hoàn toàn động từ BE** — danh sách + key + label/description/sampleSubject lấy từ `GET /admin/emails/templates`, không hardcode. BE đổi template → FE tự đồng bộ, không redeploy. **Bỏ iframe preview**; preview chuẩn = nút "Gửi test cho tôi" gửi email BE thật vào inbox admin. (3) [admin-emails.ts](src/app/actions/admin-emails.ts) viết lại: `getEmailServiceStatusAction` trả `{ smtpEnabled, templates: EmailTemplate[] }` (fallback `label=key` khi BE chưa extend); `sendTestEmailAction` bỏ `VALID_KEYS`/`EMAIL_TEMPLATE_META`, chỉ `z.string().min(1)` (BE validate key). **BE sẽ extend** response thêm `label`/`description`/`sampleSubject` — FE đã xử lý optional nên không vỡ khi chưa có. (4) **Nhãn tiếng Việt hiển thị** ([template-labels.ts](src/lib/emails/template-labels.ts)) — từ điển `EMAIL_TEMPLATE_LABELS` cho 15 key BE (lớp i18n hiển thị, KHÔNG phải mapping key). Thứ tự ưu tiên render: `label` BE → nhãn FE → key. Giúp admin đọc "Chào mừng chủ nhà" thay vì `welcome_owner` ngay cả khi BE chưa extend. Typecheck **0 errors**, lint **0 warnings**. |
| 2026-06-14 | xoá System Settings + emails-status-wire | **BE chốt KHÔNG làm API settings** (1 admin operator, default checkin/out/currency/language hardcode `constants.ts` là đủ; terms/cancelPolicy là static content landing; maintenance gấp dùng PM2/nginx 503). → **(1) Xoá hẳn trang `/admin/settings`** (Option 1): xoá [system-settings.tsx], `/admin/settings/page.tsx`, `/admin/settings/emails/page.tsx` (toàn bộ là mock `setTimeout` không persist — tránh user tưởng đã lưu). **Chuyển trang Mẫu email lên [`/admin/emails`](src/app/(admin)/admin/emails/page.tsx)** (route mới, breadcrumb/back trỏ `/admin` Tổng quan). Sidebar [manager-sidebar.tsx](src/components/layout/manager-sidebar.tsx) đổi mục "Cài đặt hệ thống" → **"Mẫu email"** (icon `Mail`) trỏ `/admin/emails`. (2) **Wire `GET /admin/emails/templates` (spec §16)** — action mới `getEmailServiceStatusAction()` trong [admin-emails.ts](src/app/actions/admin-emails.ts) trả `{ smtpEnabled, templateKeys }`. Trang emails: badge "● SMTP đang hoạt động/chưa cấu hình", truyền `smtpEnabled` xuống [test-email-button.tsx](src/components/admin/test-email-button.tsx) → disable nút + hint "(SMTP tắt)" khi `false`; cảnh báo amber khi template FE chưa có trong danh sách key BE. Lỗi/chưa expose → `undefined` = không xác định, KHÔNG chặn gửi (tránh false-block). **Decision BE đã chốt (nếu sau này làm)**: 1 object `GET/PUT /admin/settings` + `GET /settings/public` (Public), maintenance BE enforce global guard (không chỉ FE banner), terms = Markdown, không versioning (dựa AuditLog `settings.update`), currency chỉ VND, time regex `^([01]\d\|2[0-3]):[0-5]\d$`, chỉ ADMIN ghi. **Flag BE còn mở (Q7)**: 15 key spec §16 (`welcome_owner`, `booking_confirmed`...) khác bộ key FE (`booking_confirmation`, `booking_request_guest`...) — cần BE xác nhận mapping để `POST /admin/emails/test` không lệch key. Typecheck **0 errors**, lint **0 warnings**. |
| 2026-06-23 | gộp 2 trang tài chính → 1 | **Gộp `/admin/payments` (trạng thái subscription) + `/admin/payments/sessions` (duyệt thanh toán) thành 1 trang** vì tách 2 gây khó hiểu. Mô hình mới rõ: chủ nhà CK mua gói → tạo PaymentSession → admin **Duyệt** (xác nhận nhận tiền) → gói kích hoạt. [/admin/payments](src/app/(admin)/admin/payments/page.tsx) (171 dòng — chỉ điều phối) có **segmented control 2 view** qua `?view=approve|subs`: (a) **"Chờ duyệt (N)"** = hàng đợi đối soát (banner ACB + bảng session + `MarkSessionPaidButton`), default khi có session chờ; (b) **"Tất cả gói cước (N)"** = trạng thái subscription từng chủ nhà (tabs status + search + `SubscriptionRowActions`). Mỗi view có status sub-filter (`?status=`) + search (`?q=`) + phân trang (`?page=`, size 10). 2 view tách ra component [payments-approve-view.tsx](src/components/admin/payments-approve-view.tsx) + [payments-subs-view.tsx](src/components/admin/payments-subs-view.tsx), 2 bảng [subscriptions-table.tsx](src/components/admin/subscriptions-table.tsx) + [payment-sessions-table.tsx](src/components/admin/payment-sessions-table.tsx), helper chung [payments-shared.ts](src/components/admin/payments-shared.ts) (giữ mọi file <400 dòng). `/admin/payments/sessions` → `redirect('/admin/payments?view=approve')` (giữ bookmark cũ). Sidebar gộp 2 mục ("Subscription chủ nhà" + "Duyệt gói cước đã mua") → 1 mục **"Gói cước chủ nhà"** (bỏ import `Banknote` thừa). Status lọc client-side để count trên segmented control luôn là tổng. Typecheck **0 errors**, lint 0 warning mới. |
| 2026-06-23 | kyc-bypass-toggle | **Nút "Cho qua KYC" (kycBypass) ở trang chi tiết user** (spec §2A.7 — `PATCH /users/:id/kyc-bypass` body `{bypass:boolean}`, ADMIN-only, đã live). Full stack: `AdminUser.kycBypass:boolean` + `SetKycBypassInput` (entity), port `setKycBypass`, `ApiAdminUserRepository.setKycBypass` (patch rồi re-fetch withStats; map `s.kycBypass ?? false`), `setKycBypassUseCase` (Zod `{userId:uuid, bypass:boolean}`), `setUserKycBypassAction` (requireAdmin + revalidate). UI: [kyc-bypass-toggle.tsx](src/components/admin/kyc-bypass-toggle.tsx) (badge trạng thái + nút cấp/thu hồi qua `ConfirmDialog` + toast + router.refresh) gắn vào section "Xác minh KYC & Gói cước" (OWNER-only) trong [admin/users/[id]/page.tsx](src/app/(admin)/admin/users/[id]/page.tsx). Typecheck **0 errors**, lint 0 warning mới. |
| 2026-06-23 | be-v2-§25-wire | **Wire 8 endpoint mới + mở rộng (BE spec §25, đã live prod) + nối 2 tile dashboard.** **#1** `PATCH /auth/profile` — `AuthRepository.updateProfile` + `updateProfileAction` (Zod fullName/email/phone, guard `getCurrentProfile`→/login tránh circular import) + `profile-form.tsx` chuyển sang `useActionState` (bỏ setTimeout giả). **#2** đổi role: `AdminUserRepository.updateRole` → `PATCH /users/:id/role` (KHÔNG prefix `/admin`), `changeUserRoleAction` gọi `updateRoleUseCase` thật (RoleSchema `userId` siết `.uuid()`), bỏ comment "mock-only". **#3** Guests module mới (full stack): `core/entities/guest.ts`, port, 2 use case, `ApiGuestRepository` (`GET /guests` + `/guests/:id`), `guestRepository()` container, `actions/guests.ts` (guard `requireManagerRole` + uuid), trang list `/host/guests` (search `q` + filter label + phân trang server-side) + detail `/host/guests/[id]` (recentBookings), `guest-label-badge.tsx`. **#4** `GET /subscriptions/me/invoices` → entity `SubscriptionInvoice` + `listMyInvoices` + `listMyInvoicesAction` + bảng hoá đơn trong `/host/billing` (thay empty-state; SALE 400 → notice). **#5** `POST/GET /admin/subscriptions/:id/call-log` → `SubscriptionCallLog` + `addCallLog`/`listCallLogs` + 2 action (requireAdmin + uuid, note 3–2000) + `subscription-row-actions.tsx` lưu thật (bỏ toast giả). **#6** `POST /calendar/bulk` (đổi path, body `{mode, items:[{propertyId,date}]}`, max 100) → `CalendarRepository.bulkLock` + `bulkLockDatesAction` 1 request (thêm guard `requireManagerRole`, expand `dates[]`→`items[]`). **#7** `GET /users?withStats` → map `stats.disputeCount` + `lastActiveAt` (bỏ default cứng 0/null; `lastActiveAt` null = chỉ dùng web → "Chưa ghi nhận"). **#8** `GET /admin/disputes/:id` mở rộng: map `evidence`(alias attachments)/`verdict`(alias resolution text)/`chatExcerpts[]`(≤20, cũ→mới)/`penalty`(enum phẳng → `penaltyAction`); `resolveDisputeAction` thêm `penaltyAction` (Zod enum), repo `resolve` chỉ gửi `{resolution, refundAmount, penalty}` đúng spec (object penalty FE-internal KHÔNG gửi); detail page render transcript + penalty badge; form thêm selector "Mức xử phạt", ô hoàn tiền hiện khi `penaltyAction==='refund'`. **#9** `GET /admin/kyc/:id` mới → `KycAdminDetail` (uploads 3 ảnh + payments + `verificationFields[7]` + passed/total), port `getDetail`, `getKycAdminDetailAction`, trang `/admin/kyc/[id]` render checklist 7 mục + ảnh CCCD/selfie + payments (`kyc-detail-sections.tsx`). **Group B** dashboard `/admin`: "Khiếu nại đang mở" → `countActiveDisputesAction`, "Doanh thu subscription" → `sumPaidSubscriptionsAction(monthRange)` (bỏ hardcode 0 + 15.9M giả). **Review** (code-reviewer agent): 0 CRITICAL, 3 HIGH đã fix (guard `updateProfileAction` + `bulkLockDatesAction`; uuid-validate `userId` ở call-log + role). Typecheck **0 errors**, lint 0 warning mới. **Còn chờ BE/quyết định**: dispute form vẫn còn UI verdict/penaltyType FE-internal cũ (không gửi BE, chỉ `refundAmount` dùng) — nên dọn ở đợt sau; KYC score giả định thang 0–1; `lastActiveAt` chỉ từ mobile FCM. |
| 2026-06-23 | mock-layer-removal | **Loại bỏ hoàn toàn lớp Mock (dự án thật chỉ dùng API).** **Nhóm A — đã xoá:** (1) Xoá 12 file `src/infrastructure/mocks/*` + thư mục `mocks/`. (2) [container.ts](src/infrastructure/container.ts) viết lại: gỡ helper `modeFor`, type `Mode`, mọi nhánh rẽ `?mock:api` + singleton (lead/subscription/review/audit-log chỉ tồn tại để cache state mock) → mỗi hàm `return new ApiXxxRepository()`. Gỡ 12 import Mock. (3) Xoá 6 biến `NEXT_PUBLIC_DATA_MODE_*` khỏi `.env` + `.env.local` (mặc định vốn đã `api`, ghi `=api` thừa). (4) Sửa comment lỗi thời nhắc Mock repo trong [audit-log.ts](src/core/entities/audit-log.ts), [review.ts](src/core/entities/review.ts), [calendar-repository.ts](src/application/ports/calendar-repository.ts), [entitlement.ts](src/lib/entitlement.ts), [properties/new/page.tsx](src/app/(host)/host/properties/new/page.tsx). (5) Cập nhật CLAUDE.md §1/§2/§3/§6/§7/§9/§10. **Nhóm B — GIỮ LẠI (không phải mock repo, là stub vì BE chưa có endpoint — xoá sẽ vỡ UI):** [profile-form.tsx](src/components/account/profile-form.tsx) (chưa có `PATCH /auth/profile`), [admin-users.ts](src/app/actions/admin-users.ts) `updateUserRoleAction` (chưa có `PATCH /admin/users/:id/role`), [lead.ts](src/app/actions/lead.ts) legacy mock-success, 2 tile "(mock — chờ BE)" ở [admin/page.tsx](src/app/(admin)/admin/page.tsx), email test mode (`NEXT_PUBLIC_EMAIL_LIVE=false`). Khi BE ra endpoint → wire thẳng vào action. **Trạng thái mới**: 0 file mock, container chỉ Api. Typecheck **0 errors**, lint chỉ còn warning pre-existing (unused `profile`/`user` + eslint-disable thừa), không phát sinh mới. |
| 2026-06-13 | be-v1.14-sync | **Đồng bộ BE v1.14 (NO_SHOW + risk-kpis + delete grace).** (1) **Booking NO_SHOW (status=4)**: `BookingStatus` thêm `'no_show'`; `ApiBookingRepository.STATUS_MAP[4]='no_show'`; label "Khách không đến" + badge variant `dark` trong [booking-display.ts](src/lib/booking-display.ts) + 3 Record cục bộ (host list/detail, đã đồng bộ). Thêm tab "Khách không đến" ở `/admin/bookings` (lọc client-side) + `/host/bookings` (lọc server `?status=no_show`) + `isBookingStatus`/`VALID_STATUS`. (2) **`/admin/reports` → `GET /admin/reports/risk-kpis` thật (spec §7A.5)**: full stack mới — entity [risk-kpi.ts](src/core/entities/risk-kpi.ts) (`RiskRange`, `RiskMetric{value,prev}`, `TopHostCancelRow`, `RiskKpis` 10 field), port `DashboardRepository.getRiskKpis(range)`, `ApiDashboardRepository` map tolerant (`metric()` chấp nhận `{value,prev}`/số/thiếu), use case [get-risk-kpis.ts](src/application/dashboard/get-risk-kpis.ts), action `getRiskKpisAction(range)` (requireAdmin). Trang hiển thị **8 KPI card có delta vs kỳ trước** (Dispute mở, Chủ huỷ %, No-show %, Review cờ, Yêu cầu xoá account, KYC chờ, Subscription quá hạn [snapshot — ẩn delta], Doanh thu đã thu [tăng=tốt]) + **range tabs** 7/30/90/365 ngày + bảng **Top chủ huỷ** thật (từ `topHostCancel`) + bảng Dispute đang chờ (`listDisputesAction`). Bỏ KPI ghép tạm từ count rời (đợt trước). (3) **DELETE /users/me grace 30 ngày**: web manager **không có UI self-delete** (chỉ trang legal `/legal/privacy` nhắc tới) → không cần đổi UX; chỉ ghi nhận (mobile xử lý). Typecheck **0 errors**, lint **0 warnings**. **Optional chưa làm**: cột "Huỷ bởi <role>" + lý do trong booking detail (BE đã trả `cancelledByRole/cancelledReason`) — chưa cấp thiết. |

---

## 12. Liên kết nội bộ

- [`docs/CODING-STANDARDS.md`](./docs/CODING-STANDARDS.md) — SOAP/RISE + design patterns chi tiết
- [`api-spec-website-admin.md`](./api-spec-website-admin.md) — Spec API đầy đủ
- [`README.md`](./README.md) — Hướng dẫn chạy project
