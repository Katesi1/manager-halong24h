# Production env for Manager (host + admin)
NEXT_PUBLIC_API_BASE_URL=https://api.halong24h.com
NEXT_PUBLIC_SITE_URL=https://manager.halong24h.com
NEXT_PUBLIC_SALE_CALENDAR_URL=https://sale.halong24h.com

# Mock mode flags — ALL must be 'api' or empty in production
NEXT_PUBLIC_DATA_MODE_BOOKINGS=api
NEXT_PUBLIC_DATA_MODE_CALENDAR=api
NEXT_PUBLIC_DATA_MODE_PAYMENTS=api
NEXT_PUBLIC_DATA_MODE_KYC=api
NEXT_PUBLIC_DATA_MODE_DISPUTES=api
NEXT_PUBLIC_DATA_MODE_PROPERTIES=api

# DO NOT set DEV_BYPASS_AUTH=1 — that's dev only!
# SEPAY webhook
SEPAY_WEBHOOK_SECRET=GENERATE_RANDOM_64_HEX
